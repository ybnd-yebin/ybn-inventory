# 리드타임 확정표

최종 갱신: 2026-07-15
근거: 정오더 6,672건 실측 (2026년 1~7월) + 7/14·7/15 납품서 교차검증

---

## 정의

**리드타임 n일 = 발주일 당일을 포함한 영업일 n일**

```
출고일 = 발주일 + (n − 1) 영업일
발주일 = 출고일 − (n − 1) 영업일
```

- 달력일 아님. 영업일 기준.
- 영업일 = 주말·한국 공휴일 제외.
- 오더예상일이 주말·공휴일이면 직전 영업일로 당김.

---

## 품목별 리드타임

| 품목군 | 리드타임 | 표본(n) | 일치율 | 상태 |
|---|---|---|---|---|
| 글래스도어 (정규격) | 3일 | — | — | 예빈 지정값 |
| 빌트인 (B/I 슬림파우더장·현관오픈장·TV장·4단유리도어) | 3일 | — | — | 예빈 지정값 (라벨 부재로 실측 불가) |
| EURO700 정규격 | 5일 | 3,258 | 99.9% | 실측 확정 |
| EURO700 비규격 | 7일 | 1,303 | 99.9% | 실측 확정 |
| EURO500 정규격 | 5일 | 921 | 100% | 실측 확정 |
| EURO500 비규격 | 7일 | 250 | 100% | 실측 확정 |
| 유리장식벽장·키큰장 **정규격** | 5일 | 380 | 100% | 실측 확정 |
| 유리장식벽장·키큰장 **비규격** | **6일** | 211 | 100% | 실측 확정 |
| AL프레임 에어힌지 / 빗각 / 빗면 / KB / 미러장식 | 7일 | 324 | 100% | 실측 확정 |
| (비)글래스 채널도어 · 연장 | 5일 | 23 | 100% | 실측 확정 |
| 그 외 정규격 (기본값) | 5일 | — | — | 기본값 |
| 바스 (드림하이텍) | 4일 | — | — | 표시 전용 (아래 참조) |

전체 예외 4건.

---

## 규칙 순서 함정 ⚠️

`leadDays()` 안에서 **에어힌지 / 빗각·빗면 / KB 규칙은 반드시 `유리장식|장식장|키큰장` 규칙보다 먼저** 검사해야 한다.

`AL프레임 에어힌지 키큰장유리도어`가 '키큰장'에 먼저 걸려 5일로 오판된 사고가 있었다.

```js
// 올바른 순서
if(/에어힌지|AL프레임/.test(n)) return 7;
if(/빗각|빗면/.test(n))        return 7;
if(/\bKB\b|미러장식/.test(n))   return 7;
if(/유리장식|장식장|키큰장/.test(n)) return isNonStd(n) ? 6 : 5;
```

---

## 비규격 판정

```js
function isNonStd(name){
  const n = String(name||'');
  return /비규격/.test(n) || n.trim().startsWith('(비)');
}
```

`(비) 글래스 채널도어 450_연장`처럼 '비규격' 문자열이 없는 표기가 실재한다. `(비)` 접두를 반드시 포함할 것.

---

## 유리장식 5일/6일 "분산"의 정체

과거 유리장식 리드타임이 5일과 6일로 흔들리는 것처럼 보였으나, 편차가 아니라 **정규격(5일)과 비규격(6일)이 섞여 있던 것**이었다. 분리하니 양쪽 다 100% 일치.

---

## 납품서 → 출고일 환산

**납품요청일 = 출고일 − 2영업일**
**출고일 = 납품요청일 + 2영업일**

실측: 납품요청일 → 출고일 영업일수(요청일 포함) 3일이 93% (6,642/7,128).

---

## AS오더 분리 ⚠️ 필수

| 참조문서번호 | 구분 | 리드타임 |
|---|---|---|
| `51*` | 정오더 | 위 표 적용 |
| `54*` | AS오더 | **랜덤 (1~13일). 정해진 값 없음** |

**리드타임 실측·검증 시 `54*`는 반드시 제외.** AS 457건 제외 후 일치율 90%대 → 100%.

---

## 공휴일 (2026)

```
2026-01-01
2026-02-16, 02-17, 02-18
2026-03-01, 03-02
2026-05-01   ← 근로자의날. 646건 검증: 일치율 75.7% → 99.7%
2026-05-05
2026-05-24, 05-25
2026-06-06
2026-07-17   ← 제헌절. 18년 만에 공휴일 재지정 (시행 2026-05-11, 금요일이라 대체공휴일 없음)
2026-08-15, 08-17
2026-09-24, 09-25, 09-26
2026-10-03, 10-05
2026-10-09
2026-12-25
```

⚠️ **2026-05-06은 영업일.** 공휴일에 넣으면 일치율이 99.7% → 83.9%로 악화한다. 추가 금지.

---

## 주말 규칙

- **오이도만 토요일 출고가 있다.** 예시 파일에 토요일 칸이 실재하고 값이 있음 (7/18 토 450=6, 8/8 토 450=2). 일요일은 칸만 있고 전부 0.
- **리드타임 계산은 주5일 그대로. 전 창고 동일.** 토요일 병합은 대조 단계에서만 한다.
- 두 질문(출고일 결정 / 표시 위치)을 분리해야 "센다·안 센다" 혼란이 풀린다.

⚠️ 토요일을 앞으로 당길 때 **직전 금요일 고정이 아니라 직전 영업일**로 가야 한다.
7/18(토) → 7/17(금)은 제헌절이라 물량이 증발했다. 7/16(목)이 맞다.

```js
function pullWeekendToPrevBusinessDay(iso){
  let cur=iso, guard=0;
  while(!isBusinessDay(cur) && guard<15){ cur=addDays(cur,-1); guard++; }
  return cur;
}
```

---

## 바스 (드림하이텍) — 별도 취급

- 리드타임 4일은 **'직원 예고편용 발주예상' 표시에만** 사용한다.
- **납품요청일 −2영업일 같은 대조 규칙은 바스에 적용 금지.**
  바스 출고일은 우리가 임의 지정하므로 리드타임 역산 자체가 무의미하다.
- 바스는 거래처 창고가 재고를 관리하고, 월 1회만 실사로 수량이 확인된다.
  낱개 출고 납품서가 우리에게 없어, 예시 파일의 당일 칸(asof == shipDate)을
  출고 추정치로 삼아 누적하는 방식으로 대응한다.

---

## 검증 불가로 남는 것

- **빌트인 3일** — 실측 데이터에 라벨이 없어 검증 불가. 예빈 지정값 사용.
- **바스 4일** — 드림하이텍, 라벨 없음. 출고일 임의 지정이라 역산 무의미. 표시값 전용.

---

## 미해결

- **글래스도어 500 수기표 6 vs 계산 11** — 기준 불일치. 원인 미규명.
  (450은 해결: 7/14~7/18 합산 14로 정확히 일치. 토요일 6개가 제헌절 버그로 누락됐던 것)
- **예시 파일 날짜 라벨 절삭** — (오이도) 파일은 `07.14` 형태로 절삭 없음 확인.
  그레이박스·바스 파일도 같은지 미확인. 다르면 파일별 파싱 분기 필요.
- **발주일 기준점** — 납품서에 발주일 필드가 없다. `납품일`은 발행일이지 발주일이 아니다.
  전날 저녁(자사 영업종료 후) 오더가 다음날로 찍히면서 ±1일 오차가 생기는 것으로 추정.
  발주일이 기록된 원본(한샘 시스템 오더 접수 화면 등) 확보 시 재검증할 것.

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-07-15 | 5/1 근로자의날 추가. 유리장식 비규격 6일 분기 신설. `(비)` 접두 판정 추가. 주말당김 공휴일 버그 수정. |
| 2026-07-15 | 전 품목 '발주일 포함' 영업일 정의로 통일. |
| 2026-07-09 | 리드타임 4종 초기 검증 (7/8 발주분 기준). |
