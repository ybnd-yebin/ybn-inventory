# 확인 대기 — 사장님 답이 있어야 넘어가는 것들

> **이 파일이 미제의 정본입니다.** 다른 문서에는 한 줄 포인터만 두고, 내용은 여기에 모읍니다.
> 새 대화방에서도 이어지도록 `CLAUDE.md` 맨 앞에서 이 파일을 가리킵니다.
>
> 마지막 정리: **2026-09-08**

---

## 이 파일을 쓰는 법

- **막히면 여기 적고, 답을 받으면 여기서 지웁니다.** 지울 때 확정값을 정본 문서(`CLAUDE.md` 등)에 먼저 옮깁니다.
- **추측으로 채우지 않습니다.** 답이 없으면 앱이 조용히 계산하지 말고 화면에 '확인 필요'로 드러내야 합니다.
- 각 줄에 **어디에 영향을 주는지**를 같이 적습니다. 답이 왔을 때 어디를 고칠지 바로 찾으려는 것입니다.
- 답을 받은 것은 지우지 말고 맨 아래 **「답 받은 것」** 으로 내립니다. 나중에 근거를 되짚을 때 씁니다.

---

## 🔴 급한 것 — 날짜가 걸려 있음

| # | 무엇 | 왜 필요한가 | 물어본 날 |
|---|---|---|---|
| A1 | **외주 에토프 첫 입고 예정 수량·사이즈** | 9/8 시작 · 9/14 본격. 받는 화면을 미리 만들어 둬야 합니다 | 이전 |

---

## 🟠 비규격 작업지시서 — 도면·치수

대상 파일: `비규격작업지시서.html`

| # | 무엇 | 지금 상태 / 왜 막혔나 | 물어본 날 |
|---|---|---|---|
| B1 | **HD B 계열 유리 세로 치수** | 확인 필요 52품목 중 30개 넘게 이 답 하나로 풀립니다 | 이전 |
| B2 | **WT 도어높이 1253 미만**의 a·b 간격 | 힌지 **4개는 확정**. 간격 규칙만 없습니다. 도면 표의 적용 범위가 1253~1473이라 그 아래는 비어 있습니다. 도면 왼쪽 `(640~860)` 표기가 표와 안 맞는데, 이게 짧은 도어용인지도 같이 봐야 합니다 | 2026-09-08 |
| B3 | **WT 도어높이 1473 초과**가 오는지 | 온다면 a가 384인지 320인지. 지금은 1473일 때만 384 | 2026-09-08 |
| B4 | **WTA**도 WTAP처럼 예외인지 | WTAP은 비대칭(`110/X/485/320/110`)으로 확정됐습니다. WTA는 아직 | 2026-09-08 |
| B5 | **BC1A·BC1B 납품 세로 910 이외** | 910은 HD 764 / OB 732로 확정. 다른 세로는 규칙이 없어 앱이 '확인 필요'로 띄웁니다 | 2026-09-08 |
| B6 | **BD2R 상부마이다** — 고정값인지 장 길이에 따라 바뀌는지 | 겸해서 **HD 하부가 350인지 380인지**. 앱과 캐드 도면 메모가 다릅니다 (`CLAUDE.md` 11-9 3-C) | 이전 |
| B7 | **BD3U 소마이다** — 고정인지 변환인지 | 겸해서 **A값(폭 395~445 = 127 / 455~595 = 138)이 앱에 아예 없습니다.** 누락일 수 있습니다 (`CLAUDE.md` 11-9 3-D) | 이전 |
| B8 | **X 치환 3종** — `WHBT`(상부변환) · `B(HD)` · `BDI1R` | 도면의 X 자리에 무슨 값이 들어가는지 모릅니다. 그 전까지 원본 그대로 둡니다 (`CLAUDE.md` 11-7) | 이전 |
| B9 | **제품명 띠 정규화 매핑** | 신형 / 구형 / EO500 을 어떤 표기로 통일할지 (`CLAUDE.md` 11-9 4) | 이전 |
| B10 | **BLD2R 캐드 도면** (가로 서랍 마이다) | 계산·규칙은 정상이고 **그림만** 임시로 `서랍(마이다)` 상자가 나갑니다. 캐드 도면이 생기면 라이브러리에 넣습니다 (`CLAUDE.md` 12-8) | 이전 |

---

## 🟡 앱 · 재고

| # | 무엇 | 왜 필요한가 | 물어본 날 |
|---|---|---|---|
| C1 | **1액형 금액 손해**를 계산할 단가 기준 | 사이즈별입니까, 장당 하나입니까 | 이전 |
| C2 | **인쇄유리를 어디 한 곳에서만 셀지** | 인쇄실 장부 vs 조립실. 두 곳에서 세면 계속 어긋납니다. 지금 당장 정하지 않아도 됩니다 | 이전 |
| C3 | **500 수기표 6 vs 11 불일치** | 어느 쪽이 맞는지 (`CLAUDE.md` 10) | 이전 |
| C4 | **엑셀 라벨 확인** | 무엇을 맞춰 봐야 하는지 (`CLAUDE.md` 10) | 이전 |
| C5 | **한샘 단가 마스터 원천 자료** | 자료 자체가 없습니다. 찾아 주시면 그 형식에 맞춰 시작합니다 | 이전 |

---

## 🟢 확인만 하면 되는 것 (답이 짧음)

| # | 무엇 | 왜 |
|---|---|---|
| D1 | **비규격 글래스도어 5영업일**을 `리드타임.md`에 넣을지 | 실측은 10건 중 8건이 5영업일인데 `리드타임.md`에는 정규격 3일만 있습니다. 한 줄만 확인하면 넣습니다 (`CLAUDE.md` 12-7) | 

---

## ✅ 답 받은 것

| 언제 | 무엇 | 답 | 어디에 들어갔나 |
|---|---|---|---|
| 2026-09-08 | WT 힌지 **380 vs 384** | **384** | `CLAUDE.md` 12-2 |
| 2026-09-08 | WT 힌지 개수 | **늘 4개** (1253 미만도) | `CLAUDE.md` 12-2 |
| 2026-09-08 | WTAP도 일반 WT 규칙으로 넣는지 | **아니오 — 예외 유지.** 비대칭 `110/X/485/320/110`, `X = 도어높이 − 1025` | `CLAUDE.md` 12-2 |
| 2026-09-08 | BC1A·BC1B 프레임 세로 | 납품 910 → **HD 764 / OB 732**, 보링 상부 142 · 하부 110, 도면은 B 공용 | `CLAUDE.md` 12-2-1 · 코드 반영 완료 |
