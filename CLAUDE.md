# CLAUDE.md — 와이비앤 납품/재고 관리 앱

이 파일은 이 저장소에서 작업하는 Claude를 위한 프로젝트 지침서입니다.
아래 규칙은 지금까지 확정된 내용이며, **기본 동작보다 우선**합니다. 규칙을 바꾸기 전에는 반드시 먼저 확인을 받습니다.

> 📌 **문서 자리 (2026-09-08 정리)**
>
> **뿌리 — 늘 보는 것 넷.** 대화를 시작할 때 함께 보십시오.
>
> | 파일 | 무엇 |
> |---|---|
> | `CLAUDE.md` (이 파일) | **규칙 정본.** 확정된 계산·날짜·코딩 규칙 |
> | **[`확인대기.md`](확인대기.md)** | **미제 정본.** 사장님 답이 있어야 넘어가는 것들. 막히면 여기 적고, 답을 받으면 여기서 지우고 정본에 옮깁니다 |
> | [`할일.md`](할일.md) | 할 일 — 지금 / 다음 / 나중 |
> | [`리드타임.md`](리드타임.md) | 리드타임 수치 정본 |
>
> **[`인계/`](인계/README.md) — 앱별 인계 문서.** 새 대화방에서 그 앱을 이어 만들 때만 꺼냅니다.
> 비규격작업지시서 · 수납도어 · 도면빌더 · 출고예시분석 · 바스재고. 어느 것을 꺼낼지는 `인계/README.md`.
>
> **`보관/` — 낡아서 치운 것.** `*.bak`, 옛 체크리스트, 재고 백업. **여기 있는 값은 근거로 쓰지 마십시오.**
>
> **규칙 세 줄**
> 1. **숫자는 한 곳에만.** 두 곳에 두면 반드시 어긋납니다. 인계 문서에 수치를 새로 적지 마십시오.
> 2. **답을 모르면 추측해 채우지 말고 `확인대기.md`에 적습니다.** 앱도 조용히 계산하지 말고
>    화면에 '확인 필요'로 드러냅니다.
> 3. **미제는 `확인대기.md`에만.** 다른 문서에는 "→ 확인대기.md B3" 같은 포인터만 둡니다.

---

## 1. 회사 구조

- **와이비앤디**와 **드림하이텍** 두 법인 체계입니다.
- **생산(공장)은 한 곳**입니다. 즉, 법인은 둘이지만 실제 생산 라인은 단일 거점에서 돌아갑니다.
- 발주서·납품서에는 법인이 나뉘어 찍히더라도, 생산·재고·리드타임 계산은 하나의 공장 기준으로 봅니다.

---

## 2. 리드타임

> 📄 **정본은 [`리드타임.md`](리드타임.md) 입니다.** 실제 수치·표본·일치율·규칙은 그 파일이 단일 출처입니다.
> 숫자를 두 곳에 두면 어긋나므로, 여기에는 요약만 두고 상세는 반드시 `리드타임.md`를 봅니다.
>
> ⚠ **아래 값은 눈금이지 확정 규칙이 아닙니다.** 어느 정도 규칙은 있지만 변동도 있어,
> 같은 계통 안에서 2일~12일까지 흔들린 실적이 있습니다.
> **계산의 기준점은 매번 납품서에 적힌 날짜**로 잡습니다 — 자세한 우선순위는 **12-7**을 보십시오.

- **정의:** 리드타임 n일 = **발주일 당일을 포함한 영업일 n일**. `출고일 = 발주일 + (n−1) 영업일`.
- 달력일 아님. 영업일 = 주말·한국 공휴일 제외. 오더예상일이 주말·공휴일이면 **직전 영업일**로 당김(3번·6-4 참고).
- 핵심 값(요약):
  - 글래스도어 정규격 3일 · 빌트인 3일(지정값)
  - EURO700/500 **정규격 5일 · 비규격 7일**
  - 유리장식벽장·키큰장 **정규격 5일 · 비규격 6일** (6-3 참고)
  - 에어힌지 / 빗각·빗면 / KB / 미러장식 **7일**
  - 바스(드림하이텍) 4일 — **표시 전용** (역산·−2영업일 대조 적용 금지)
- `54*`(AS오더)는 리드타임 실측·검증에서 **반드시 제외** (4번 참고).

---

## 3. 공휴일 규칙

- 리드타임·출고일·납품요청일 계산은 **주말(토·일) + 공휴일을 제외한 영업일** 기준입니다.
- 확정된 조정 사항:
  - **5/1(노동절) 은 휴일** — 공휴일 목록에 포함. (`KR_HOLIDAYS`에 `2026-05-01` 있음)
  - **5/6 은 영업일** — 휴일 아님. 공휴일 목록에 넣지 말 것.
  - **7/17 은 휴일** — 제헌절, 18년 만에 공휴일 재지정(시행 2026-05-11). 금요일이라 대체공휴일 없음.
    > ✅ 2026-08-26 반영 완료. `KR_HOLIDAYS`에 `2026-07-17` 들어감. `7/18(토) → 직전 영업일 7/16(목)` 확인.
- 공휴일 데이터는 `index0713.html`의 `const KR_HOLIDAYS = new Set([...])` 에 관리합니다.
- 영업일 가감은 `_addBizDays(dateStr, n)`, 휴일 보정은 `_toBizDay(dateStr, dir)`, 휴일 판정은 `_isHoliday(d)` 를 사용합니다. **직접 날짜 계산을 새로 만들지 말고 이 함수들을 재사용**합니다.

### 3-1. 날짜를 글자로 적을 때 — `toISOString()` 쓰지 말 것

- **`날짜.toISOString().slice(0,10)` 는 한국에서 하루가 밀립니다.** 세계표준시로 바꿔 적기 때문에, 로컬 자정으로 만든 날짜가 전날이 됩니다.
  ```js
  new Date('2026-08-25T00:00:00').toISOString().slice(0,10)   // → '2026-08-24'  ← 하루 손실
  ```
- **반드시 `_ymdLocal(d)` 를 씁니다.** 연·월·일을 로컬 그대로 읽어 적습니다. `todayStr()` 도 이걸 씁니다.
- 실제 사고 (2026-08-26 수정):
  - `_addBizDays`·`_toBizDay`·`_addDays` 가 결과를 하루 이르게 반환 → **출고일이 전부 +1영업일이 아닌 +0으로** 나옴.
  - `_isHoliday` 가 공휴일 목록을 하루 어긋난 날짜로 조회.
  - `todayStr()` 이 **오전 9시 이전에 어제 날짜**를 반환 → 아침 작업이 전날로 기록됨.
  - `_addBizDays('2026-08-28',1)` 이 **일요일(8/30)** 을 내놓기도 했음.
- **예외 (그대로 두는 곳):** 날짜만 있는 문자열(`new Date('2026-08-25')`)은 UTC 자정으로 파싱되므로 `toISOString()` 이 맞습니다. `_normDate` 의 '2/31 거르기' 비교와 엑셀 시리얼(`(v-25569)*86400*1000`) 변환이 여기 해당합니다.
- 시각(instant)을 적는 `createdAt`·`updatedAt`·`at` 등의 **전체 타임스탬프는 `toISOString()` 그대로** 씁니다. 문제는 `.slice(0,10)` 로 **날짜만** 뽑을 때입니다.

---

## 4. 오더 번호 규칙 (접두사)

- **AS오더 `54*` 는 제외** — 리드타임·집계·인쇄유리 소요 등에서 빼야 합니다.
  - 현재 코드: `if((d.refNo||'').startsWith('54')) return; // AS 제외`
- **정오더 `51*` 가 정상 주문** — 이게 집계·생산 대상입니다.
- 향후에는 접두사를 코드 곳곳에 하드코딩하지 말고 **`isBas()` 같은 판별 함수 하나로 모을 것** (아래 6번 참고).

### 4-1. 비규격 판정 (`isNonStd`)
- 비규격 여부는 **판별 함수 하나로** 봅니다:
  ```js
  function isNonStd(name){
    const n = String(name||'');
    return /비규격/.test(n) || n.trim().startsWith('(비)');
  }
  ```
- **`(비)` 접두를 반드시 포함할 것.** `(비) 글래스 채널도어 450_연장`처럼 **"비규격" 문자열이 없이 `(비)`로만** 표기된 품목이 실재합니다. `/비규격/`만 검사하면 놓칩니다.

---

## 5. 납품요청일 ↔ 출고일 공식

- **출고일 = 납품요청일 + 2영업일** (주말·공휴일 제외)
- 뒤집으면: **납품요청일 = 출고일 − 2영업일**
- 코드: `_shipOf(d)` → `delivReqDate`가 있으면 `_addBizDays(d.delivReqDate, 2)`.
- 저장된 옛 `shipDate`는 신뢰하지 말고 **항상 재계산**합니다.

---

## 6. 코딩 규칙 (지향 원칙)

### 6-1. 접두사 하드코딩 금지 → `isBas()`
- `.startsWith('54')`, `.startsWith('51')` 같은 판정을 여기저기 흩뿌리지 말 것.
- **오더 종류 판별은 한 함수(`isBas()` 등)로 모아** 한 곳만 고치면 전체가 바뀌게 합니다.
- (현재는 아직 `isBas()`가 없고 `'54'` 하드코딩 상태 — 리팩터링 시 이 방향으로.)

### 6-2. 오이도/그레이 느슨한 매칭 유지
- 목적지 **"오이도"**, 색상/모델 **"그레이(그레이스 등)"** 매칭은 **의도적으로 느슨하게** 되어 있습니다.
- 표기 흔들림(공백·접두어·표기 변형)을 흡수하기 위한 것이므로 **타이트하게(정확일치로) 바꾸지 말 것.**
- 모델명 정규화는 시리즈/색상 접두어를 떼는 방식(`normalize` 계열)을 씁니다.

### 6-3. 규칙 순서 함정 & 유리장식 5일/6일 분기
- 분류/매칭 규칙은 **순서가 중요**합니다. 더 구체적인 규칙을 먼저 두어야 합니다.
- 특히 **에어힌지 · 빗각·빗면 · KB(미러장식) 를 "유리장식|장식장|키큰장"보다 먼저** 판정할 것.
  - "유리장식/키큰장"이 넓게 잡히기 때문에, 앞에 두면 에어힌지/빗면/KB가 유리장식으로 잘못 흡수됩니다.
  - 실제 사고: `AL프레임 에어힌지 키큰장유리도어`가 '키큰장'에 먼저 걸려 5일로 오판.
- **유리장식벽장·키큰장은 규격에 따라 갈립니다: 정규격 5일 / 비규격 6일.** (과거 "5·6일 분산"의 정체 = 편차가 아니라 정규격·비규격이 섞인 것)
- 올바른 순서 (정본 `리드타임.md`):
  ```js
  if(/에어힌지|AL프레임/.test(n)) return 7;
  if(/빗각|빗면/.test(n))        return 7;
  if(/\bKB\b|미러장식/.test(n))   return 7;
  if(/유리장식|장식장|키큰장/.test(n)) return isNonStd(n) ? 6 : 5;
  ```

### 6-4. 주말당김 = 직전 "영업일" (직전 금요일 고정 아님)
- 주말·공휴일 출고를 앞으로 당길 때는 **직전 금요일로 고정하지 말고 `직전 영업일`로** 가야 합니다.
- 사고: `7/18(토) → 7/17(금)`으로 당겼더니 7/17이 **제헌절**이라 물량이 증발. 정답은 `7/16(목)`.
- 공휴일이 하나라도 빠져 있으면 이 로직이 조용히 틀립니다(3번 7/17 미반영과 직결). 
  ```js
  function pullWeekendToPrevBusinessDay(iso){
    let cur=iso, guard=0;
    while(!isBusinessDay(cur) && guard<15){ cur=addDays(cur,-1); guard++; }
    return cur;
  }
  ```
- 참고: **오이도만 토요일 출고가 있습니다.** 리드타임 계산 자체는 전 창고 주5일 그대로이고, 토요일 병합은 대조 단계에서만 합니다.

---

## 7. 문서 작성 스타일

- **격식체**로 작성합니다 (안내문·공지·설명 문구 등).
- **과한 한자어는 회피** — 쉽고 자연스러운 우리말 위주로.
- 앱 내 안내 문구(`info-note` 등)도 같은 톤을 유지합니다.

---

## 8. 작업 원칙

- **한 층씩** 작업합니다. 한 번에 여러 곳을 동시에 뜯지 않습니다.
- **되돌릴 수 있게** 진행합니다. 각 단계가 롤백 가능해야 합니다.
- **하나를 완성하고 검증한 뒤 다음**으로 넘어갑니다.
- 편집 전에는 **계획부터 보여주고 승인**을 받습니다.

---

## 9. 파일 구조 & 실행 방법

- 앱은 **단일 HTML 파일**(`index0713.html`)로 동작합니다.
- 데이터는 `ybn-data.js` 에 있으며, HTML의 `<script src="...">` **파일명이 실제 데이터 파일명과 정확히 일치**해야 합니다. (불일치 시 데이터가 안 붙는 함정)

### 9-1. 로컬 실행 (중요)
- **`<script src="...">` 는 HTML 파일을 더블클릭으로 열면 동작하지 않습니다.**
  브라우저 보안 정책상 로컬 파일끼리의 참조(`file://`)가 막히기 때문입니다.
- **로컬 서버가 필요합니다.** Claude Code에 아래를 띄워달라고 요청하세요:
  ```
  python3 -m http.server
  ```
  그런 다음 브라우저에서 `http://localhost:8000/index0713.html` 로 엽니다.

### 9-2. 빌드 방식 (비규격작업지시서에 도입 중 — 2026-09-08 확정)
- 원본은 **나눠 두고**, 올릴 때 스크립트로 HTML에 자동 삽입해 **더블클릭용 단일 파일**도 같이 냅니다.
- 장점: 원본 지식은 **한 곳에만** 있고, 결과물은 **더블클릭으로도 열립니다**(인터넷 없는 곳에서도).
- 빌드는 **제가 푸시할 때 같이 돌립니다.** 사장님이 하실 일은 없습니다.

### 9-3. 파일을 나눌 때 — 그물을 먼저 치십시오 (필수)

> **딸린 파일이 안 붙었는데 조용히 넘어가는 것이 이 프로젝트의 대표 사고입니다.**
> `ybn-data.js`가 안 붙어도 아무 말 없이 모델 주의사항만 비어 나온 일이 실제로 있었습니다(9번).
> **파일을 나눌수록 이 사고가 늘어납니다. 그래서 나누기 전에 그물부터 칩니다.**

- 각 딸린 js는 **맨 끝에서 붙음 표식**을 남깁니다:
  `window.__YBN_PARTS['이름'] = { v:판, file:'파일명' }`
- 화면 쪽은 `PARTS_NEED`(파일)와 `PARTS_GLOBALS`(있어야 할 값)로 검사합니다.
  **파일을 새로 나눌 때마다 `PARTS_NEED`에 한 줄 더하십시오.**
- **필수**(없으면 결과가 틀림) → 빨간 띠 + **[생성] 단추 잠금.**
  반쪽짜리 작업지시서가 인쇄되어 현장에 나가는 것이 제일 위험합니다.
  **선택**(없어도 대부분 돎) → 노란 띠로 알리기만. (지금 `ybn-data.js`가 여기)
- 판(`v`)이 다르면 **낡은 파일이 캐시에 남은 것**이므로 그것도 알립니다(깃허브 Pages에서 흔합니다).

**만들면서 걸린 함정 셋 — 되풀이하지 마십시오**

1. **`window.RULES` 로는 못 잡습니다.** 최상위 `const`·`let`은 `window`에 안 올라갑니다.
   `typeof RULES!=='undefined'` 로 봐야 합니다(없는 이름에 `typeof`는 오류가 안 납니다).
2. **검사는 파일 맨 끝에서 부릅니다.** 앞에서 부르면 뒤쪽에 선언된 값(`SUBDOOR_SHEETS` 등)이
   아직 선언 전이라 '없다'고 **헛경보**가 납니다.
3. **검사 호출은 따로 떨어진 `<script>` 덩어리에 둡니다.**
   같은 덩어리에 두면 **화면 스크립트가 중간에 죽을 때 검사도 같이 죽어**, 아무 말 없이
   반쪽짜리 화면이 남습니다. 따로 두면 "화면이 다 안 열렸습니다"까지 잡아냅니다.

**곁들여 — `localStorage` 읽기는 반드시 `try/catch`로 감싸십시오.**
저장함이 막힌 브라우저(사생활 보호 창, 사이트 자료 차단)에서 감싸지 않은 읽기 한 줄이
**화면 스크립트를 통째로 멈춥니다.** 그러면 뒤쪽 선언이 아예 안 만들어집니다.
`initColorSettings`에서 실제로 겪었습니다(2026-09-08 수정).

---

## 10. 진행 순서 & 미해결 항목

- **한 번에 다 하지 말 것.** 위 **5번까지 먼저** 적용하고 **며칠 써본 뒤**, 괜찮으면 **6번(리팩터링·빌드)** 으로 넘어갑니다.
- **미해결로 남은 것들** (폴더 정리 끝난 뒤, Claude Code에서 처리 — 파일이 모두 그 폴더에 있어 오히려 편함):
  - **500 수기표: 6 vs 11** 불일치 확인
  - **엑셀 라벨 확인**

---

## 11. 비규격 작업지시서 — 캐드 양식 렌더러 (진행 중)

> 대상: `비규격작업지시서.html` (로컬 도구 — git에 있어도 **배포 앱 아님**, 더블클릭으로 열림).
> 목표: **기존 캐드 출력물과 구분 안 되게.** 파이프라인(파싱·비규격 전개·묶음·분할)은 유지하고 **그리는 부분만** 캐드 SVG로 교체.

### 11-1. 원칙
- 기존 `[작업지시서 생성]`(`generate`)은 그대로 두고 `[📐 캐드 양식 생성]`(`cadGenerate`)을 **병행**으로 붙였다. 새 것이 이상하면 기존으로 즉시 복귀.
- 한 단계씩, 결과 보여주고 승인 후 진행. **추측 구현 금지 — 판단 필요하면 멈추고 질문.**
- 미리보기는 base64로 뽑아 파일로 전달(로컬 `file://`에서 화면 캡처가 막힘).

### 11-2. 도면 라이브러리 (`DOOR_LIB`, `libDoor`, `libDoorFilled`)
- DXF 실측 도면 16종을 파일에 인라인. **치수(DIMENSION)까지 포함**(치수선·문자·보링마커).
- 가변치수는 문자 **X·Y·Z·A** 로 박혀 있고, 그 자리가 캐드에서 빨간 원으로 강조한 자리다.
- `libDoor(code,type)` — 코드+계열(OB/HD) 우선, 없으면 계열 폴백. 커버리지 **19/33**(나머지 파라메트릭 폴백).
- `BTARG·WTARG`는 라이브러리에만 있고 RULES엔 없음(관통보링, 별건) → **연결 안 함.**
- 각 도면 `notes` 필드에 캐드 원본 산출규칙 있음(대조용).

### 11-3. A4 좌표 (`cadSheetSVG`, 사양서 1.1)
- A4 210×297, 여백 8.75, 선 **0.18mm**, 굴림. y는 하단기준 → SVG는 `297−y`.
- 제목 **가로중앙 105**, 삽입점 y 13.02, 글자 17.5. 발주일·납품일 x **12.20 / 124.70** y 47.61 · 업체명·현장명 y 57.69.
- **머리글 글자는 5.83입니다** (사용자 확정 2026-09-15). 사양서에는 4.29로 적혀 있으나,
  수납도어 작업지시서(캐드 원본 5.83)와 나란히 놓으면 이쪽만 작아 보였습니다.
  **수납도어 크기가 기준으로 딱 좋다**고 하셔서 거기 맞췄습니다. 코드에서는 `HDR_FS` 한 곳입니다.
  넘침은 굴림으로 실제 재서 확인했습니다 — 가장 긴 줄도 오른쪽 끝 188mm로 내용 박스(201.25) 안입니다.
- 내용 박스 192.5×218.85(SVG y 69.40~288.25), 제품명 띠 아래선 SVG y 81.90.
- **재현본(작업지시서_양식재현_테스트.html)의 좌표(11.297 등)는 정본 아님** — DXF→SVG 변환 중간값. **사양서 값을 쓴다.**
- **baseline 계수 K=0.8은 사양 아님**(굴림 렌더링 세부). 사양서 좌표는 삽입점(상단)으로 취급.
- ~~D-2 줄 안 넣음~~ → **2026-09-04부터 유로700 머리글에도 D−2를 넣습니다.** 빨간 글씨이고,
  머리글 세 줄 간격은 12-3을 따릅니다(가운데 줄 y는 사양서 값이 아니라 위·아래의 중간으로 계산).

### 11-4. 제품명 띠
- **계열명만** 넣는다(수량은 블록 수량줄로). 예: `EO700 신형(OB)` · `EO500` · `EO700 구형`. 임의 형식 금지(정규화 매핑은 미확정 — 11-9).
- **매 페이지 상단 반복.** 한 페이지 안 **HD→OB 전환 시 중간 헤더 밴드**(`cadMidBanner`) 삽입 — HD 헤더 밑에 OB 섞지 않는다.

### 11-5. 페이지 분할 (`cadSplitToPages`) — 그룹을 온전히 (2026-09-09 전면 개정)

> **한 줄 규칙: 그룹을 합쳐야 장수가 줄어들 때만 합칩니다. 안 줄면 그룹 경계에서 끊습니다.**

- **왜 바꿨나**: 예전에는 남은 자리에 뒤 블록을 끌어와 채우는 **그리디**였습니다. 그래서 어차피
  3장 나올 물량인데도 `[HD+OB] [OB+500] [500]`처럼 그룹이 두 번 걸쳐 나왔습니다.
  이제 `[HD] [OB] [500]`으로 나옵니다.
- **어떻게**: 그리디 대신 **DP(전수 탐색)** 로 가장 좋은 배치를 고릅니다.
  좋고 나쁨은 이 차례로 봅니다(앞이 같을 때만 뒤를 봅니다).

  | 차례 | 잣대 |
  |---|---|
  | 1 | 총 장수가 **적을 것** |
  | 2 | **그룹이 장을 걸치는 횟수**가 적을 것 |
  | 3 | 장마다 **고르게** 찰 것 |

- 셋째 잣대는 **빈자리를 제곱해 더한 값**입니다. 장수가 정해지면 빈자리 총합도 정해지므로,
  제곱합이 작을수록 고르게 퍼진 배치입니다. **분산을 그대로 쓰면 안 됩니다** —
  평균이 전체에 얽혀 있어 DP로 쪼갤 수 없고, 최적이 보장되지 않습니다.
- 값이 모두 같으면 **블록을 더 많이 담은 쪽**(앞 장을 더 채우는 쪽)을 고릅니다.
  안 그러면 `[A][AA]`처럼 첫 장이 헐겁게 나옵니다.
- **마지막 장이 절반 넘게 비어도 정상입니다.** 억지로 채우지 않습니다.
- 상수: **`PAGE_CONTENT_HEIGHT`**(206.35) · **`PREFER_GROUP_PAGES`**(true — `false`면 예전처럼 꽉 채우기) ·
  블록 높이는 **`cadPackBlockHeight()`** 한 곳에서 냅니다(렌더러와 같은 식이어야 어긋나지 않습니다).
- 지키는 것(예전 그대로): **블록은 안 쪼갭니다.** 서로 다른 도면 **4개**(`CAD_LAYOUT.MAX_DRAWINGS`)까지.
  장 안에서 그룹이 바뀌면 머리띠(`CAD_BANNER_H=13`)를 높이에 포함합니다.
- 그룹이 다음 장으로 이어지면 그 장 머리글에 **`(계속)`** 을 붙입니다(`groupCont`).
  블록 자체가 잘렸을 때의 `(계속)`(`isCont`)과는 **다른 표시**입니다.
- **블록 하나가 한 장보다 크면** 단독으로 두고, 그래도 안 들어가면 마지막 수단으로 행을 나눕니다
  (안 나누면 내용이 박스 밖으로 잘려 나갑니다).

### 11-6. 블록 렌더 (`cadBlockSVG`, `CAD_DOOR`)
- **도면 왼쪽 크게 + 텍스트 오른쪽**(세로 가운데). 오른쪽 여백은 손메모 자리로 남긴다.
- 텍스트: 코드(**검정** 굵게) / **색상(꺾쇠)** / 완성치수=수량(검정) / 재단치수(**빨강**).
  ⚠ **색으로 뜻을 나누는 것은 빨강 하나뿐입니다** — 재단치수. 약어는 2026-09-22부터 검정입니다(12-19).
- 도면 크기 `CAD_DOOR = { TXT_SCALE: 3.5/25(치수글자 ~10pt), MAX_H: 95, MAX_W: 72, PLAIN_H: 52 }`. 값만 바꾸면 크기 일괄 변경.

### 11-7. X 치환 (`libDoorFilled` — **DOM 텍스트 노드 단위, 정규식 문자열치환 금지**)
- **확정**: `BHBU` X = 하부변환값(`bhbu.conv`) · `WTAP` X = 변환값(`wtap.conv`).
- **보류**: `WHBT`(상부변환 — 앱에 값 없음) · `B(HD)`(도면 2변형·값 불명확) · `BDI1R`(X 의미 불명확) · `TH`(변수 3개·상호참조·힌지 미확정). 그 전까지 원본 유지/폴백.

### 11-8. 계산 규칙 (`calc` — 확정, 덮어쓰기 금지)
- 프레임 = 납품 −5(가로)×−4(세로). 유리(재단) = 프레임 −0×−(손잡이 both 3/단일 1), 구형 −4×−4, 합적 −3.
- **사양서 5절(계열 감산표)로 덮어쓰지 않는다.** 손잡이 both/단일 판정(원인)이 코드계열 판정(결과)보다 정확.
- **WTAP 변환값** = −4 뺀 도어세로 fh에서 (`conv = fh − 1025`, base=fh). 납품세로 아님.
- **TH**: 프레임 fw×fh 1장(안 나뉨), 중간 손잡이 **바닥 854 고정**. 하부유리 = 가로×**851**(854−3), 상부유리 = 가로×**(fh−854−2)**. 총길이=fh. (실측 445×2271 → 851 / 1415 검증)
- **EURO500**: BD2R·BD3U 등 마이다 합적에 **EURO700과 같은 세로 고정값**(SPLIT 350·380 등), **폭만 −6**. 비-마이다는 폭−6×세로−4 그대로. 유리 없음(프레임만). (`buildEuroBody`)
- **미지 코드**(RULES에 없음, 시그니처 글래스도어 등) = 계산 말고 **목록으로 노출**. 조용히 EURO700 기본계산으로 흘리지 말 것.

### 11-9. 미완료

> **사장님 답을 기다리는 것은 [`확인대기.md`](확인대기.md)로 옮겼습니다** —
> X 매핑 3종(B8) · BD2R(B6) · BD3U(B7) · 제품명 띠 정규화(B9).
> 여기에는 **답이 필요 없는, 제가 그냥 하면 되는 것**만 둡니다.

1. **실물 피드백 미반영** (도면 그리기):
   - ~~BHBU **빨간 동그라미가 잘림 → 원이 온전히 보이게**~~ → **2026-09-09 처리 완료** — 강조원을 숫자를 감쌀 만큼으로 줄였습니다(12-4 참고).
   - BHBU 도면 **30% 축소 + 치수 글자 키우기.**
   - **TH 도면 더 키우기.**
   - ~~WTAP·WT: 치수선 세로 정렬, 빨간 원 안 잘리게~~ → **2026-09-07 처리 완료** (12-4 참고).
2. **폴백 도면**(라이브러리 없는 14코드) 파라메트릭 병행 다듬기.
3. **캐드 양식 기본 출력 전환**은 위가 정리되고 승인 후.

---

## 12. 확정 규칙 모음 (2026-09-08 정리)

> 여기 있는 값은 **실적 자료로 확인했거나 사용자가 확정한 것**입니다. 고칠 때는 근거 자료를 다시 보고,
> 눈대중으로 바꾸지 마십시오. 각 항목에 근거를 함께 적어 둡니다.

### 12-1. 감산 — 계열마다 다릅니다 (하나로 뭉뚱그리지 말 것)

**EURO700 (유로 계열)** — 정본은 11-8.

| 단계 | 가로 | 세로 |
|---|---|---|
| 프레임 = 납품 − | **−5** | **−4** |
| 유리 = 프레임 − | −0 | **−3 (양면손잡이) / −1 (단일손잡이)** |

- 유리 세로 감산은 **손잡이 개수로** 갈립니다. 코드 계열로 판정하지 마십시오(11-8).
- 구형 프레임은 예외: 유리 = 프레임 **−4 × −4**.
- EURO500은 **폭만 −6**, 유리 없음(프레임만).

**수납도어 4계통** — 출처: `C:\Users\MSI\Downloads\한샘 AL 발주 현황.xlsx`
(시트 `비규격 글래스도어` · `비규격 유리장식장` · `비규격 KB 빗각도어` · `비규격 AL프레임 에어힌지`의
D~I열 = 장규격 → 프레임 → 유리). 2026-09-07 기준 실적 400여 건이 아래와 전부 일치합니다.

| 계통 | 프레임 = 장 − | 유리 = 프레임 − | 표본 |
|---|---|---|---|
| 시그니처 글래스도어 | **W −4, H ±0** | W −5, H −5 | 81건 |
| EURO 유리장식장 | W −5, H −4 | W −24, H −24 | 198건 (전부 일치) |
| KB AL프레임 에어힌지 | W −5, H −4 | **W −41**, H −40 | 123건 |
| KB 빗면 프레임 | W −5, H −4 | W −95, H −10 | 26건 (전부 일치) |

- **시그니처만 프레임이 −4×−0** 입니다. "프레임은 −5×−4"를 전 계통에 적용하면 틀립니다.
- **에어힌지 유리 가로는 2025-11-18에 −40 → −41로 바뀌었습니다.** 그 전 25건은 −40,
  그 뒤 98건(2026-09-03까지)은 모두 −41. 규격표 머리글도 `-41*-40`. 옛 발주를 다시 뽑을 일이 있으면
  이 전환점을 기억하십시오.

### 12-2. WT 힌지 — 늘 4개, 간격만 갈립니다 (사용자 확정 2026-09-08)

> 출처: WT 캐드 도면(사용자 제공 2026-09-08). 도면에 **적용 범위 `도어높이 1253~1473`** 이 적혀 있습니다.

- **WT 계열은 힌지가 항상 4개**입니다. 예전에 적어 둔 "1473일 때만 4개"는 **틀렸습니다.**
  1473은 힌지 개수가 아니라 **간격 a**를 가르는 값입니다.
- 배치는 **위·아래 대칭** `110 / a / b / a / 110` 입니다. 대칭이라 어느 쪽에서 읽어도 같습니다.

| 도어높이(= 프레임 세로) | a | b (중간) |
|---|---|---|
| 1253 ~ 1472 | **320** | **도어높이 − 860** |
| **1473** | **384** | **485** |

- **b는 나머지입니다.** `110+320+320+110 = 860` 이라 `b = 도어높이 − 860`.
  1473도 같은 이치 — `110+384+384+110 = 988`, `1473 − 988 = 485`.
  **숫자를 손으로 적지 말고 계산해 쓰십시오.**
- 기준은 도면 표기 그대로 **"도어 실높이" = 프레임 세로(fh)** 입니다. 납품 세로가 아닙니다.
  (`calc()` 안 `isWT && fh===1473`) 납품 1477 − 4 = 프레임 1473.
- **384로 확정됐습니다.** 캐드 도면 메모의 `110-380-X-380-110`(380)은 쓰지 않습니다.
  (11-9의 3-B "WT 힌지 380 vs 384" 항목은 이걸로 닫힙니다.)
- 보링 사양(도면): 힌지보링 `ø35 × 11.5`, 사전보링 `ø7.8 × 11.5`.
- **도어높이 1253 미만도 힌지 4개**입니다(사용자 확정 2026-09-08). 도면 표의 적용 범위는 1253부터지만
  개수는 아래로도 4개입니다.
- **WTAP은 예외 그대로 — 비대칭입니다.** (WTAP 캐드 도면으로 확인 2026-09-08)
  위에서 아래로 `110 / X / 485 / 320 / 110`, **아래쪽만 320**입니다.
  `110+485+320+110 = 1025` → `X = 도어높이 − 1025`. 지금 앱의 `conv = fh − 1025` 와 같으니 **그대로 두십시오.**
  "그 외 모든 WT는 110/320/…"에 **WTAP은 들어가지 않습니다.**
- ⏸ 남은 미제는 **[`확인대기.md`](확인대기.md) B2·B3·B4** — 1253 미만의 a·b, 1473 초과, WTA.

### 12-2-1. BC1A · BC1B — 세로는 고정값, 도면은 B와 공용 (사용자 확정 2026-09-08)

- **프레임 세로는 −4로 계산하지 않습니다. 고정값이고, 타입에 따라 갈립니다.**

  | 타입 | 납품 910 → 프레임 세로 |
  |---|---|
  | **HD** | **764** |
  | **OB** | **732** |

- 가로는 −5, 유리는 0 × −3 그대로입니다.
- **보링: 상부 142 · 하부 110.**
- **도면은 B 코드의 캐드도면을 같이 씁니다.** 전용 도면을 새로 만들지 마십시오.
  **코드 자리는 `DOOR_ALIAS`** — `{ W2HBT:'M', BC1A:'B', BC1B:'B' }`. `libDoor()`가 제 도면을 못 찾으면
  이 표를 보고 돌립니다. 도면 없는 코드를 다른 코드로 돌릴 때는 **여기 한 줄만** 더하십시오.
  (✅ 2026-09-08 반영. 그전에는 도면이 없어 파라메트릭 네모 상자로 나갔습니다.)
  HD(764)가 B의 프레임 세로(910→764)와 같은 값이라 자리가 맞습니다. B도 `hingeBase 764` · `topB 142`입니다.
- 납품 세로가 910이 아닌 것은 규칙을 아직 못 받았습니다. 계산하지 말고 **'확인 필요'로 드러냅니다.**
  ([`확인대기.md`](확인대기.md) B5)
- ✅ **코드 반영 완료 (2026-09-08).** `비규격작업지시서.html`의
  `RULES.BC1A/BC1B` → `hSpecial:{HD:{910:764}, OB:{910:732}}` 와, 이를 읽는 `calc()`.
  `calc()`는 `hSpecial`이 `{HD:…,OB:…}` 꼴이면 타입으로 먼저 고르고 **옛 평면 꼴(`{910:732}`)도
  그대로 동작**합니다. 검증: `BC1A 445×910 HD → 440×764` · `OB → 440×732`,
  회귀 `B 340×910 → 335×764` · `WT 595×1477 → 590×1473` 이상 없음.

### 12-3. 머리글 — D−2 는 빨간 글씨, 현장명은 맨 아래

**줄 차례 (사용자 확정 2026-09-08) — 캐드 원본 DXF와 같은 배치입니다.**

```
                        D - 2 : 26. 09. 14 (월)   ← 빨강
발주일 : 26. 09. 08 (화)   납품일 : 26. 09. 16 (수)
업체명 : 와이비앤디        현장명 : 한샘 비규격     ← 현장명이 맨 아래
```

- **`D - 2` 로 한 칸씩 벌리고, 콜론 앞은 두 칸입니다** — `D - 2␣␣: ` (`D-2` 아님, 한 칸도 아님).
  캐드 원본 DXF가 그렇습니다(수납도어 도면 `SUBDOOR_SHEETS`의 `{{D2}}` 자리와 같은 글자열).
  굴림에서 한글 세 글자(`납품일`)는 3em인데 `D - 2`는 반각 다섯 글자라 2.5em뿐입니다.
  **칸 하나가 그 0.5em을 메워야 콜론·날짜가 아랫줄과 앞이 맞습니다.**
  한 칸으로 줄이면 D−2 줄만 왼쪽으로 밀려 보입니다. (사용자 지적 2026-09-08)
- **왼쪽맞춤 줄은 '보이는 글자'의 왼쪽 끝을 맞춥니다.** 같은 x에 놓으면 어긋나 보입니다 —
  굴림은 글자마다 왼쪽 여백이 달라 `D`·`현`은 0.09em, `업`은 0.12em, `납`·`발`은 0.13em부터
  잉크가 시작합니다. `T()`가 첫 글자를 재서(`_inkLeftEm`) 한글 기준(0.13em)까지 밀어 줍니다.
  **가운데맞춤(제목·제품명 띠)에는 적용하지 않습니다.**
- **D−2는 빨강 `#e00000`.** 캐드 양식(`cadSheetSVG`)과 수납도어 도면(`SUBDOOR_SHEETS`) **양쪽 모두**.
- D−2 = **납품요청일**. 납품일 = 납품요청일 + 2영업일 (5번과 같습니다).
- 오른쪽은 위에서 **D−2 → 납품일 → 현장명** 차례입니다. 현장명과 납품일을 바꿔 놓지 마십시오.
- 세 줄은 **간격을 똑같이** 둡니다 — 맨 위(D−2)와 맨 아래는 사양서 자리 그대로,
  가운데 줄은 `(위+아래)/2`로 계산합니다. 숫자를 손으로 적지 마십시오.

### 12-4. 캐드 양식 배치 — 어긋나기 쉬운 자리

- **블록 사이 여백은 `CAD_BLOCK_GAP` 한 곳에서만** 정합니다(현재 5mm).
  쪽 나누기(`cadSplitToPages`)와 렌더(`cadPageSVG`)가 같은 값을 봐야 합니다.
  예전에 두 곳에 `7`을 따로 적어 두어, 어긋나면 내용이 박스 아래선을 넘었습니다.
- **계열(띠)을 건너뛰어 블록을 끌어오지 않습니다.** 페이지를 채우려고 뒤 블록을 당겨 올 때
  같은 계열 안에서만 찾습니다. 안 그러면 700 → 500 → 다시 700으로 섞입니다.
  계열 순서는 한 방향(HD → OB → EURO500)으로만 넘어갑니다.
- ~~치수 숫자는 눕히지 않고 바로 씁니다.~~
  → **2026-09-09부터 눕혀서 안쪽에 넣습니다** (사용자 확정, 앞 규칙을 뒤집음).
  숫자를 **90° 눕혀 치수선 안쪽**(도어 쪽)에 붙입니다. `rotate(-90)`이라 아래에서 위로 읽힙니다(캐드와 같은 방향).
  - **왜 바꿨나**: 바로 쓰면 글자가 치수줄 바깥으로 뻗어 도면이 옆으로 넓어지고,
    블록 오른쪽 글자 자리를 먹었습니다.
  - 눕히면 글자가 세로로 서서 서로 밀어낼 일이 거의 없으므로,
    **밀어 둔 자리(ny)가 아니라 제 치수칸 한가운데(ny0)** 를 그대로 씁니다.
  - 스위치는 **`CAD_DIM_ROTATE`** 한 곳뿐입니다. `false`로 바꾸면 옛 방식으로 즉시 돌아갑니다.
- **빨간 강조원은 숫자를 감쌀 만큼으로 줄입니다** (눕히기와 함께 2026-09-09).
  캐드 원본의 강조원은 반지름이 100 남짓이라 도면 밖으로 크게 튀어나가고,
  그만큼 배치 칸을 먹어 **도어가 작게** 그려졌습니다. (11-9의 "빨간 동그라미가 잘림"과 같은 뿌리)
- **치수선은 곧게.** 캐드 원본은 한 치수줄인데도 조각마다 x가 어긋나 있습니다(WTAP 483.24 / 490.52).
  가장 긴 조각의 x로 모두 맞춥니다.
- **도어 바깥에 떠 있는 보조선은 도어 끝까지 당겨 붙입니다.** 도어를 이미 물고 있는 선은 건드리지 않습니다
  (건드리면 도어 안쪽으로 파고듭니다).
- 글자 길이는 어림하지 말고 **실제로 잽니다**(`_dimTextW`). 굴림 숫자는 0.574em이라,
  예전의 `0.62 × 글자수` 어림이 8% 부풀려져 "겹친다"는 오판을 만들었습니다.

### 12-4-1. 오른쪽 꺾쇠 — 색상과 세트를 줄 대신 옆에 적습니다 (2026-09-09)

블록 오른쪽은 손메모 자리로 비워 두던 곳입니다. 거기에 **세로 꺾쇠**를 세워
색상과 세트를 적으면 **줄을 벌 수 있습니다.**

```
415 × 764 = L1/R1  ┐
415×761            ┘ ● 에토프
325 × 764 = L1     ┐
325×761            │ ○ 웜코튼
365 × 764 = R2     │
365×761            ┘
```

- **색상 꺾쇠** (`CAD_COLOR_AS_BRACKET`) — 색상 줄을 없애고 오른쪽으로 옮깁니다.
  **색마다 한 줄씩** 법니다. 색 사이 숨 돌릴 틈(0.5줄)은 그대로 둡니다.
  색이름은 **둥근 사각형에 색을 채우고 그 안에 글씨**로 넣습니다(2026-09-09).
  앞 동그라미는 뺐습니다 — 칩이 색을 알려 주므로 겹칩니다.
  배색은 다른 화면과 같은 표(`COLOR_CHIP`)를 씁니다(배경 `c.fill` · 글씨 `c.text`).
  글자 너비는 **한글 1.0em · 그 밖 0.55em**으로 잽니다 — `W/Cotton`처럼 영문으로 와도 칸이 맞아야 합니다.
- **세트 수는 꺾쇠에 안 적습니다** (2026-09-09 사용자 확정). 색상 꺾쇠가 이미 그 색의 줄을
  묶어 주므로 세트 꺾쇠는 겹쳤습니다. 대신 이렇게 적습니다.

  | 블록 안 세트 수 | 어디에 |
  |---|---|
  | 모두 같고 두 벌 이상 | **코드 옆에 `×2SET`** (빨강) |
  | 색마다 다름 | **그 색 칩 뒤에 `×2SET`** — 이때는 `×1SET`인 색에도 적습니다 |
  | 모두 한 벌 | 안 적습니다 |

  ⚠ **한 벌짜리 색도 `setmark`를 반드시 내보내야 합니다.** 안 내보내면 '색마다 같은지'를
  잘못 봅니다 — 웜코튼 2SET · 에토프 1SET인데 표식이 하나뿐이라 **제목에 `×2SET`이
  잘못 찍힌** 적이 있습니다(2026-09-09 수정). 화면에 실제로 적을지는 렌더러가 정합니다.
  또 `titleSet`은 **코드줄보다 먼저** 잡아야 합니다(코드줄이 그 값을 씁니다).
- 되돌리려면 `CAD_COLOR_AS_BRACKET = false`.

⚠ **꺾쇠는 줄 높이를 먹지 않습니다.** `cadTextH`에서 `color`·`setmark`를 빼야 하고,
렌더러도 그 줄에서 `ty`를 올리면 안 됩니다. **두 곳이 어긋나면 내용이 박스 아래선을 넘습니다.**
실제로 `setmark`를 `ty+=lh` 뒤에 두어 한 줄만큼 어긋난 적이 있습니다(2026-09-09 수정).
고친 뒤에는 **블록 높이 실제값과 `cadTextH` 계산값이 같은지** 반드시 재 보십시오.

### 12-4-2. 인쇄 — 캐드 양식은 여백 0, 옛 양식은 10mm (2026-09-10)

- **캐드 양식 한 장은 정확히 210mm** 입니다. 그런데 `@page` 여백이 10mm였던 탓에
  브라우저가 **210mm를 190mm로 줄여** 넣어 전체가 약 10% 작게 찍혔습니다.
- 이름 붙인 페이지로 **캐드 양식에만** 여백 0을 줍니다.
  ```css
  @page      { size:A4; margin:10mm; }   /* 옛 양식 등 나머지 */
  @page cad  { size:A4; margin:0; }      /* 캐드 양식 전용 */
  @media print{ .cad-page{ page:cad; } }
  ```
- ⚠ **옛 양식(`.sheet-tbl`)까지 0으로 바꾸면 안 됩니다.** 쪽 나누기(`paginateSheets`)가
  `PAGE_MARGIN_MM = 10` 으로 높이를 재고 있어, 한꺼번에 바꾸면 쪽이 어긋납니다.
  두 값은 **반드시 같아야** 합니다.
- 종이 가장자리에 붙지 않습니다 — 캐드 양식은 SVG 안에서 이미 **8.75mm**를 띄워 테두리를 그립니다.
- 인쇄 창에서 **배율 100% · 여백 기본**이어야 합니다. '맞춤(Fit to page)'이면 브라우저가 다시 줄입니다.

⚠ **한 장은 정확히 297mm여야 합니다 — 조금이라도 넘치면 빈 장이 끼어듭니다.**
SVG는 그냥 두면 `display:inline` 이라 **아래에 글줄 여백이 붙어** 한 장이 **298.59mm**가 됐습니다.
여백이 10mm였을 때는 브라우저가 통째로 줄여 넣어 안 드러났는데, 여백을 0으로 바꾸자
그 1.59mm가 다음 장으로 넘어가 **2장짜리가 5장으로** 나왔습니다(2026-09-10).
막는 법: `.cad-page{line-height:0;font-size:0}` · `.cad-page>svg{display:block}` ·
인쇄에서는 `.cad-page`와 그 안 svg를 **210mm × 297mm로 못 박습니다.**
여백을 건드린 뒤에는 **한 장의 실제 높이를 재서 297mm보다 작은지 확인하십시오.**

### 12-4-3. 도면 선·치수 숫자 굵기 (2026-09-10)

캐드 원본은 선이 가늘어 인쇄하면 흐릿합니다. 두 상수로 한꺼번에 조절합니다.

| 상수 | 값 | 무엇 |
|---|---|---|
| **`CAD_LINE_MM`** | **0.25** | **종이 기준 선 굵기(mm)** — 도면마다 여기에 맞춥니다 |
| `CAD_LINE_BOLD` | **1.30** | 거기에 곱하는 배수 → 실제 **0.325mm** |
| `CAD_DIM_BOLD` | **1.30** | 치수 숫자를 30% 굵게 |
| `CAD_DIM_STEM_EM` | 0.08 | 굴림 숫자 획 두께(글자크기 대비) — 굵힘 폭의 기준 |

- **선이 흐리면 `CAD_LINE_MM` 만 올리십시오.** 도면 하나만 고치면 다시 제각각이 됩니다.
- ⚠ **도면마다 굵기가 제각각이었습니다** (사장님 지적 2026-09-18 "M 도면이 되게
  흐리멍텅해 보이네"). 캐드에서 들어온 굵기를 그대로 썼기 때문입니다 — 실측으로
  **M 0.184mm · BLD1UA 0.325mm** 로 두 배 가까이 벌어져 있었습니다.
  이제 `cadEmbedDoor` 가 **가장 굵은 선이 `CAD_LINE_MM` 이 되게** 도면마다 맞춥니다.
  글자를 `DIM_MM` 에 맞추는 것과 같은 방식이라, **도면 안의 굵기 차이**(치수선이
  더 가는 것)는 비율 그대로 살아 있습니다. 18종 전부 0.325mm 로 맞는 것을 확인했습니다.
- 굵기를 못 받은 선의 기본값도 종이 기준으로 냅니다. 예전에는 `CAD_LINE_BOLD`(1.3)를
  그대로 적어, viewBox 가 큰 도면에서는 있으나 마나 한 굵기였습니다.
- 숫자는 `font-weight`로 못 굵힙니다 — 굴림에 중간 굵기가 없어 400에서 700으로 확 건너뜁니다.
  그래서 **글자 테두리에 제 색으로 얇은 선**을 둘러 조금씩 두껍게 합니다.
- 굵기를 못 받은 선(`stroke-width`가 아예 없는 것)도 같이 굵어지도록,
  바깥 `<svg>`에 기본 굵기를 걸어 둡니다.
- ⚠ **숫자 굵히기는 `libDoorDimTextOnLine` 뒤에서** 해야 합니다. 그 함수가 치수 숫자를 눕히면서
  속성을 다시 쓰기 때문에, **앞에서 테두리를 붙이면 조용히 지워집니다.**
  실제로 겪었습니다 — 선은 굵어졌는데 숫자만 그대로였습니다(2026-09-10).

### 12-5. 이름이 겹치면 조용히 깨집니다

- 한 파일 안에 같은 이름의 함수를 두 번 선언하지 마십시오. 나중 선언이 앞 것을 덮어씁니다.
- 실제 사고: `cadBlockH`가 미리계산용과 렌더러용으로 두 번 선언되어 있었고, 미리계산이 렌더러용을
  부르며 없는 값을 읽어 매번 예외로 죽었습니다. 화면이 안 바뀌니 **직전 옛 양식이 그대로 남아**
  버그로 보이지도 않았습니다. (지금은 `cadPreviewBlockH`로 분리)

### 12-6. 납품서에서 사이즈 읽기 — `(비)` 표기 함정

- 사이즈를 뽑는 `delivSizeLR`이 이름에 `비규격` 글자가 있어야만 동작했습니다.
  시그니처는 `(비) 글래스 채널도어 450_연장`처럼 **`(비)`로만** 옵니다(4-1과 같은 함정).
- 지금은 마지막 수단으로 **괄호로 온전히 닫힌 별표형 숫자묶음**을 읽습니다.
  닫는 괄호를 요구하므로, 칸 너비에 잘려 나간 조각(`(450*0*73`)은 여전히 안 읽습니다.

### 12-7. 납기 — 기준은 언제나 **납품서에 적힌 날짜**입니다

> **리드타임 표는 규칙이 아니라 눈금입니다.** 어느 정도 규칙은 있지만 변동도 있습니다.
> 그래서 **계산의 기준점은 매번 납품서에 적힌 날짜**로 잡습니다. 표에서 역산한 값으로 덮어쓰지 마십시오.
> (사용자 확정 2026-09-08)

**우선순위 — 이 순서를 바꾸지 말 것**

1. **납품서의 납품요청일** → `ship`. 이 날이 우리 기준일입니다. (`parseDelivText`의 `reqDate`)
2. **납품서의 구매오더 발행일** → `po`(발주일). 납품서에 찍혀 있으면 **그대로 씁니다.**
3. 납품서에 발주일이 없을 때**만** `bizBefore(ship, lead)`로 **추정**합니다.
   추정값은 참고용이고, 실제 값이 오면 언제나 실제 값이 이깁니다.
4. 납품일(출고일)은 5번 공식대로 `납품요청일 + 2영업일`로 **늘 다시 계산**합니다.

- 앱은 이미 이 순서로 동작합니다(`od = 납품서 po ... || bizBefore(ship, lead)`).
  새 화면을 만들 때도 같은 순서를 쓰십시오. 리드타임 표를 1순위로 올리면 안 됩니다.
- 리드타임 표(`리드타임.md`)의 쓰임새는 **① 발주일이 비었을 때의 추정, ② 이상치 걸러내기,
  ③ 계획·문의 대응**입니다. 납품서 값을 교정하는 용도가 아닙니다.

**실측 — 변동 폭이 이 정도입니다** (워크북 `라벨납기날짜`, 발주일 포함 영업일)

| 계통 | 가장 많은 값 | 그 밖에 나온 값 | 표본 | `리드타임.md` |
|---|---|---|---|---|
| 비규격 글래스도어 | **5영업일** (8건) | 8일(2) | 10 | 정규격 3일 |
| 비규격 유리장식장 | **6영업일** (23건) | 2·3·4일 | 41 | 정규격 5 / 비규격 6 ✓ |
| 비규격 AL프레임 에어힌지 | **7영업일** (11건) | 2·3·4·5·12일 | 23 | 7일 ✓ |
| 비규격 KB 빗각도어 | 5일(2) / 3일(2) | — | 4 | 7일 |

- 유리장식장 6일 · 에어힌지 7일은 `리드타임.md`와 **맞습니다**. 표가 틀린 게 아니라
  **같은 계통 안에서도 2일~12일까지 흔들린다**는 뜻입니다. 그래서 1번 원칙이 필요합니다.
- 비규격 글래스도어 5일도 **`리드타임.md`에 이미 있습니다** — `(비)글래스 채널도어 · 연장 5일`
  (23건 100% 실측 확정). 위 표의 10건보다 표본이 크니 그쪽을 정본으로 보십시오.
  (2026-09-15 확인. 그전에는 "없는 값"이라고 잘못 적혀 있었습니다.)

### 12-8. 수납도어 탭 — 도면은 손으로 고치지 않습니다

- `비규격작업지시서.html`의 **🚪 수납도어** 탭. 시그니처 글래스도어 · EURO 유리장식장 ·
  KB AL프레임 에어힌지 · KB 빗면 프레임, 도면 11장(`SUBDOOR_SHEETS`).
- 도면 SVG는 `C:\Users\MSI\Desktop\한샘 비규격 수납도면 작업지시서(클2).dxf` 아래쪽 11장을
  통째로 옮긴 것입니다(**1mm = 캐드 14.20942단위**, A4 210×297, 내용 박스 192.5×218.85 — 11-3과 일치).
  **SVG를 손으로 만지지 말고 원본 DXF에서 다시 뽑으십시오.**
- 채우는 자리는 이것뿐입니다: `{{PO}}` 발주일 · `{{DELIV}}` 납품일 · `{{SITE}}` 현장명 ·
  `{{D2}}` D−2 · `{{F1}}` 프레임(검정) · `{{G1}}` 유리(빨강). 시그니처만 450연장·500연장
  두 줄이라 `{{F2}}`·`{{G2}}`가 한 벌 더 있습니다.
- **계통 판별은 `subdoorGroup()` 한 곳에서만** 하고, **에어힌지 · 빗면을 유리장식보다 먼저** 봅니다
  (6-3과 같은 함정 — 이름에 '키큰장'·'벽장'이 겹칩니다).
- 유리장식키큰장 도면은 **프레임 세로 2240 이하 → 첫 도면(2114~2240), 2241 이상 → 둘째 도면(2242~2286)**.
- 맞는 도면이 없으면 조용히 흘리지 말고 **화면에 목록으로 드러냅니다**(11-8 끝줄과 같은 원칙).
- 좌우가 섞인 발주의 수량은 한 줄에 **슬래시로 이어** 적습니다 — `= L1/R2` (2026-09-11 변경).
  띄어쓰기(`L1 R2`)로 두면 두 값인지 한 값인지 한눈에 안 들어옵니다. 캐드 양식 블록도 같은 표기입니다.
  좌우가 적힌 게 있는데 안 적힌 것이 섞이면 `L1/R2/미표기1` 로 **드러냅니다**(숨기지 않습니다).
  좌우가 하나도 없으면 예전처럼 `3 EA`.
- **같은 도면에 사이즈가 여러 개면 한 장에 줄만 늘립니다** (사용자 지적 2026-09-10).
  예전에는 장을 묶는 열쇠에 `가로x세로`까지 넣어, **같은 유리장식벽장인데 사이즈가 둘이면
  장이 둘로** 갈렸습니다. 도면이 같으니 나눌 이유가 없습니다.
  - 열쇠는 `납품요청일 | 도면` 까지만 봅니다. 시그니처만 세로를 남깁니다(세로가 다르면 배치가 다름).
  - `p.slots[n]` 은 이제 **값 하나가 아니라 목록**입니다. 첫 사이즈를 꺼낼 때는 `subdoorFirst(p)`.
  - 줄은 `<tspan dy>` 로 늘립니다. 간격은 숫자를 손으로 적지 말고
    **캐드가 잡아 둔 `{{F1}}`↔`{{G1}}` 의 y 차이를 그대로 재서**(`subdoorLineStep`) 씁니다 —
    도면마다 달라서, 하나로 정해 두면 어긋납니다.
  - 한 장에 **`SUBDOOR_MAX_SIZES`(6)** 까지. 넘으면 잘라 냅니다.
- **날짜 고르기는 「발주일」 기준입니다** (2026-09-11 변경 — 예전에는 납품요청일).
  한 번에 낸 발주를 통째로 뽑는 것이 일하는 단위와 맞습니다.
  **장을 묶는 것은 예전대로 납품요청일별**이라, 한 발주일에 요청일이 여럿이면 장이 나뉩니다 —
  머리글의 D−2·납품일이 요청일마다 달라야 하기 때문입니다.
  - **차례는 최신이 맨 위, 기본값은 가장 최근 발주일입니다** (사장님 요청 2026-09-22).
    탭을 열면 바로 오늘 것이 보입니다. 날짜 없는 것(`(발주일 없음)`)은 맨 아래입니다.
    ⚠ **「날짜 다시 읽기」는 고르신 것을 건드리지 않습니다** — `subdoorFillDates(true)`.
    보시던 중에 최신으로 튀면 곤란합니다. `전체`도 고르신 것으로 봅니다.
- **ㄱ자 후크(코너캡) 모서리는 그릴 때 둥글립니다** (사용자 요청 2026-09-11).
  실물은 라운드로 마감되는데 캐드는 각으로 그려 놓았습니다.
  **도면 데이터는 손대지 않습니다** — `subdoorRoundSheet()`가 그릴 때만 호로 바꿉니다.
  캐드에서 다시 뽑아도 그대로 먹고, 되돌리려면 `SUBDOOR_ROUND_R = 0`.
  - 무엇을 둥글릴지: **가로·세로가 4~20mm이고 두 변이 1.5mm 이상**인 꺾임만.
    도어 몸통(63mm)·프레임 상자(41mm)는 크고, 단면 아이콘 잔선(0.05mm)은 작아서 걸러집니다.
  - 되돌아가는 자리(겹쳐 그린 선)는 건드리지 않습니다 — 캐드가 그렇게 뽑아 둔 것입니다.
  - 실제로 걸리는 것: 에어힌지 3종 각 2개(10.6×10.6) · KB 벽장 1개 · KB 키큰장 3개. 나머지는 0개.
- **종이 가장자리 테두리는 그릴 때 뺍니다** (사장님 요청 2026-09-22 "위에 라인은 잘려서
  나와. 아예 겉에 라인 없애버려"). 캐드 원본 맨 앞에 **종이 크기 그대로의 네모**(0,0 → 210,297)가
  들어 있는데, 프린터는 가장자리에 못 찍으므로 **늘 잘려 나옵니다** — 특히 윗줄.
  `subdoorDropPageFrame()` 이 **그릴 때만** 뺍니다. 도면 데이터는 그대로입니다.
  되돌리려면 `SUBDOOR_DROP_PAGE_FRAME = false`.
  - 가리는 법: **직선(M·L·Z)으로만 된 경로** 중 **가로 205 이상 · 세로 290 이상**.
    가로·세로를 **둘 다** 봐야 안쪽 내용 박스(192.5 × 218.85)가 안 지워집니다.
  - 곡선(A)이 섞인 경로는 건드리지 않습니다 — 숫자가 좌표가 아니라 반지름일 수 있습니다.
    그래서 **둥글리기(`subdoorRoundSheet`)보다 먼저** 부릅니다.
  - 실측(2026-09-22): **11장 전부 바깥 네모 1개씩만** 빠지고 내용 박스는 전부 남습니다.

- 아직 도면이 없는 코드: **BLD2R**(가로 서랍 마이다). 규칙·치수 계산은 정상이고
  그림만 `서랍(마이다)` 상자로 대신 나갑니다. 캐드 도면이 생기면 라이브러리에 넣으십시오.

### 12-8-1. 구형 유로700 — 네 색, 사이즈표는 규격표가 정본 (사용자 확정 2026-09-15)

- **구형은 네 색입니다: 사하라 · 미드나잇 · 포레스트 · 화이트.** 전에는 앞 둘만 봤습니다.
- 구형은 정규격이라도 **비규격 작업지시서로** 나갑니다. 프레임이 신형과 달라 치수를 찍어 줘야 합니다.
  **도면은 안 붙입니다 — 표로만** 나갑니다(사용자 확정).
- **감산**: `프레임 = 납품 −5 × −4` · `유리 = 프레임 −4 × −4`. (12-1 구형 예외와 같은 말입니다)
- **사이즈표 정본은 `메인 작업양식/EO700_그레이스_규격_드림하이텍(210423) …xlsx` 의 `700규격` 시트**입니다.
  시트의 **'제품 사이즈(SizeH×SizeV)' 가 곧 프레임**이고, 유리는 거기서 −4×−4 입니다.
  **111줄 전부가 이 관계와 맞습니다**(어긋난 줄 0 — 2026-09-15 전수 확인).
  그래서 앱에는 프레임만 적고 유리는 계산해 씁니다. 숫자를 두 곳에 두지 않습니다.
- ⚠ **키는 약어＋번호입니다.** 번호만 쓰면 **6군데가 충돌**합니다 —
  `6091` 만 해도 `BAD 595×586` · `BD3U 595×173` · `BLD1R 595×250` · `BD2R 595×350`.
- **구형 네 색은 모두 EURO700 으로 나옵니다. 오프화이트는 EURO500 입니다** (사용자 확정 2026-09-15).
  그래서 판별은 둘만 봅니다: **① EURO500 이 아닐 것 ② 색이 네 가지 중 하나일 것.**
  넓게 잡아도 안전합니다 — EURO700 색은 여덟 가지뿐이고(Grace ETP · W/Cotton · Sahara ·
  Forest · Veil Shade · BRZ · PT/BK · RW) 그중 'white' 가 들어가는 신형 색이 없습니다.
- ⚠ **제품코드 접두로 가리지 마십시오.** 한때 "구형은 `MR`로 시작한다"고 잡았는데, 납품서
  465장을 세어 보니 이렇습니다.

  | 접두 | 라인 | 색 | |
  |---|---|---|---|
  | `MRS` · `MRT` | EURO700 | 포레스트 · 사하라 | **구형** |
  | `MRN` | EURO700 | 베일/러프우드 | 구형 아님 |
  | `MRU` · `MRV` · `MRW` | EURO500 | 오프화이트 · 아보카도 · 로즈그레이 | 구형 아님 |

  `/^MR/` 로 잡으면 **EURO500 오프화이트 264건이 통째로 구형 치수로 실립니다.**
- ✅ 2026-09-15 반영. `비규격작업지시서.html` 의 `OLD_STD_SIZE`(111줄) · `oldStdOf()` ·
  `isOldText()`. 검증: 납품서 `5400162466`(AS) 의 `M 3073(공용) M` → 295×732 · 291×728,
  `M 4573(공용) M` → 445×732 · 441×728. 옛 키 `6062` 도 그대로 595×350 · 591×346.
  판별은 열두 경우로 확인했습니다 — EURO500 오프화이트·아보카도는 안 걸리고,
  Grace White·미드나잇은 걸리고, 신형(ETP·웜코튼)과 베일(RW·BRZ·PT/BK)은 안 걸립니다.

### 12-8-2. 캐드 블록은 **코드가 아니라 그림으로** 묶습니다 (사용자 확정 2026-09-16)

> **한 줄 규칙: 그림이 같으면 한 덩어리. 그림은 한 번만 그리고, 줄마다 그것이 무엇인지를 적습니다.**

- **왜**: 캐드 원본(DXF)부터 **여러 코드가 도면 한 장을 함께 씁니다.**
  `WF · WFED · WFMD · WFUP · WHFM · WHMH · WRMB · WRMBS` 여덟 코드가 한 도면이고,
  `WMLDT · M` 도 한 도면입니다. 코드로 나누면 **똑같은 그림이 코드 수만큼 되풀이**됩니다.
  구형 정규격이 모델번호(`M 3073` · `M 4573`)마다 갈리던 것도 뿌리가 같았습니다.
- **열쇠는 `cadDrawKey(r)`** — 라이브러리 도면이 있으면 그 도면(코드묶음＋계열),
  없으면 예전처럼 코드입니다. 도면이 없는 코드는 그릴 때 **코드별 파라메트릭 도면**이
  나오므로 묶으면 서로 다른 그림이 한 덩어리가 됩니다.
- **줄머리 표시** (`cadBlockRows` 의 `lab`) — 줄을 새로 쓰지 않고 치수줄 **왼쪽 칸**에 적습니다.

  | 무엇 | 언제 적나 |
  |---|---|
  | **코드** | 한 덩어리에 코드가 **둘 이상일 때만** (하나뿐이면 제목줄과 겹칩니다) |
  | **모델명** (구형 정규격) | **늘 적습니다** — 규격표 몇 번 줄인지가 종이에 남아야 합니다 |

  ```
  WFUP · WF                        ← 제목줄: 이 도면을 쓰는 코드를 모두
    WFUP  1045×356 = 2EA   ┐
          1045×355         │ 브리즈
    WF    1045×372 = 2EA   │
          1045×371         ┘
  ```
- **안 묶는 것**: 계열이 다르면(HD / OB / EURO500 — 띠가 다름) · **정규격(구형) ↔ 비규격**
  (같은 코드 `M` 이라도 `3565`(구형 정규격)와 `520×736`(비규격)은 다른 물건입니다).
- **화면의 확인·수정 표는 코드별로 그대로 둡니다.** `WF`는 가로장·힌지 상단인데 `WFUP`은
  그 표시가 없습니다 — **그림은 같아도 조립 안내가 다릅니다.** 캐드 출력물에는 안내문이
  안 찍히니 묶어도 잃는 것이 없지만, 표에서까지 묶으면 그 차이가 가려집니다.
- **색은 색순으로** 나열합니다(`cadColorOrder`). 차례는 `COLOR_CHIP` 에 적힌 순서 그대로 —
  화면 색칩과 같은 표라 두 곳이 어긋나지 않습니다. 표에 없는 색은 뒤에 가나다순.
- ⚠ **줄머리는 줄 수를 바꾸지 않습니다.** 바꾸면 `cadTextH`(장 나누기)와 어긋나 내용이
  박스 아래선을 넘습니다(12-4-1 과 같은 함정). 대신 글자가 길어진 만큼 **오른쪽 꺾쇠가
  밀리므로**(`BX_SET` 에 `LABW` 를 더합니다) 고친 뒤 **실제로 재서** 확인하십시오.
- ✅ 2026-09-16 반영·실측: 오른쪽 끝 최대 **201.25mm 안**, 한 장 **정확히 297mm**,
  아래끝 264mm. `cadDrawKey` · `cadColorOrder` · `cadBlockRows` · `cadBlockSVG` · `cadGenerate`.

### 12-9. 예시 vs 실물 대조 — 분류·제외 규칙 (사용자 확정 2026-09-09)

> 대상: `출고예시분석.html` 「납품서 대조」 탭의 **예시 vs 실물 대조**.
> 검증: 2026-09-07 출고예시 3종 + 2026-09-08 납품서 5장. 아래 값이 모두 재현됩니다.

**AS오더(54\*) — "실물로 세지 않되, 소진 확인에는 쓴다"**

CLAUDE.md 4번의 '제외'는 아래 표의 범위입니다. 4번을 고치는 것이 아니라 범위를 밝힌 것입니다.

| 용도 | AS 포함 |
|---|---|
| 실물 총수량 · 미예고율 분모 | **제외** |
| 미예고 판정 | **제외** (AS는 예시에 없는 게 정상. 넣으면 미예고율이 통째로 왜곡됨) |
| 조기발주 판정 | **제외** |
| 헛스윙 판정 | **참조** — 예시 항목이 AS로라도 나갔으면 헛스윙이 아님 (`AS 출고` 표시) |

- 헛스윙만 예외인 이유: 헛스윙의 뜻은 **'만들었는데 안 나감 = 재고 리스크'** 입니다.
  AS로 나갔으면 실제로 소진된 것이므로 리스크가 아닙니다.
- **예시 엑셀에는 AS 구분이 없습니다.** 상위제품(AS) 코드·명칭 칸이 전 행 공란이고
  오더유형도 '전체오더' 단일값입니다(3개 파일 확인). 그래서 **AS 판별은 납품서 참조문서번호 `54*` 로만** 됩니다.
- **AS 대조는 '코드＋창고'로만** 봅니다(사이즈까지 맞추지 않음). 납품서 PDF는 제품명이 칸 너비에
  잘려 옵니다 — 실제로 `비규격 (500*0*1 - 내용참조` 처럼 숫자 한가운데서 끊겨
  500×1349가 500×1로 읽혔습니다. AS는 '실제로 나갔나' 확인용이라 코드 단위로 충분합니다.

**바스 — 대조 대상이 아닙니다**

- 바스(드림 예시에 있고 와이비 코드셋에 없는 코드)는 **한샘 납품서에 나오지 않습니다.**
  예시에 남겨두면 전부 가짜 헛스윙이 됩니다. 우연이 아니라 규칙입니다.
- **대조 기준 예시 수량 = 예시 전체 − 바스.** (2026-09-08 실측: 77 − 9 = 68)

**대조 키 = 코드 ＋ 사이즈 ＋ 창고**

- 코드만으로 묶으면 같은 코드의 다른 사이즈가 한 줄로 뭉개져 **사이즈변경을 영영 못 봅니다.**
- 제품명 전체를 키로 쓰지 않는 이유: 예시(엑셀)와 납품서(PDF)의 표기가 다르고 PDF는 잘려 옵니다.
  두 자료에서 똑같이 뽑히는 것은 `비규격(...)` 안의 사이즈뿐입니다(`sizeSig`).
- 정규격은 사이즈 표기가 없고 코드 하나가 한 규격이라 코드＋창고로 충분합니다.
- `ingestDeliv`의 품목 합치기도 같은 키를 씁니다. 코드로만 합치면 여기서 이미 뭉개집니다.

**다섯 분류 + 장(EA) 단위 정합**

| 분류 | 뜻 | 색 |
|---|---|---|
| 일치 | 예시 = 실물 | 무채색 |
| 납품서에 없음 | 예시에 있고 실물에 없음 | **빨강** |
| 예시에 없음 | 예시에 없고 실물에 있음 | **주황** |
| 조기발주 | 예시에는 있으나 더 뒤 발주일 것이 앞당겨 나옴 | 무채색 |
| 수량차이 | 양쪽에 있으나 수량이 다름 | 무채색 |

- **색은 빨강·주황 둘뿐입니다.** 늘리면 어느 것이 급한지 안 보입니다. 라이트 모드 고정.
- 수량은 장 단위로 쪼개 나누어 **합이 맞아떨어져야 합니다**:
  `실물 = 일치 + 미예고 + 조기발주 + 오이도초과` · `예시 = 일치 + 헛스윙 + AS소진`
- **미예고율 = 미예고 ÷ 실물(정오더).**
- **오이도는 창고 보충**이라 예시 대비 수량초과가 정상입니다. 메인 표에서 빼 화면 아래
  「오이도 창고물(참고)」로 따로 두고 **경고하지 않습니다.**
- **사이즈변경 의심**: 같은 코드·같은 가로·세로만 다른 헛스윙↔미예고 짝.
  **헛스윙에서만 빼고 미예고는 그대로 둡니다** — 미예고는 '예고 없이 왔다'는 사실 자체가
  남는 위험이라, 지우면 미예고율이 실제보다 좋아 보입니다.

**2026-09-08 확정 실측값** (재현 검증용 · 위 규칙이 맞는지 되짚을 때 쓰십시오)

```
실물(정오더) 71 · 예시(바스 제외) 68 · AS 참조 18 · 바스 제외 9
일치 59 (그레이 57 + 오이도 2) · 헛스윙 7 · 미예고 9 · 조기발주 0
오이도 수량초과 3 · AS 소진 2 · 미예고율 12.7%
사이즈변경 의심 1 적용 시 → 헛스윙 6 (ME1WUX007 560×736 ↔ 560×836)
```

**따로 쌓는 지표 — AS 항목 중 예시에 있던 비율** (오늘 18장 중 2장)
이 값이 낮게 유지되면 AS는 예시에 거의 안 실린다는 뜻이고, 그때는 **AS 역참조 로직 자체가
불필요해집니다.** 판단은 2~3주 뒤에 합니다. 미예고율과 함께 `ybn-match-trend-v1`에 일자별로 쌓입니다.

### 12-10. 납품요청일 = 출고일 − 2영업일 — 정오더 4/4 일치 (2026-09-09 정정)

- 5번 공식(`출고일 = 납품요청일 + 2영업일`)은 2026-09-08 납품서에서 **정오더 4건 전부 일치**했습니다.
- 예전에 적힌 "5건 중 4건, 예외 1건"은 **AS를 정오더에 섞어 센 오류**입니다.
  **AS는 애초에 이 규칙의 적용 대상이 아닙니다**(12-9 표 참고).
- 엑셀 날짜 칸은 **출고일**, 납품서는 **납품요청일**입니다. 리드타임 규칙은 출고일 기준이므로
  두 자료를 견줄 때 축을 반드시 맞추십시오.

**낱말 (2026-09-09 변경)** — 화면에 쓰는 이름은 `헛예시` · `예시 밖` 입니다.
「헛스윙」은 야구 비유라 **무엇에 대한 헛스윙인지가 이름에 없어** 와닿지 않았습니다.
두 분류 모두 '예시'를 기준으로 한 사건이므로 이름도 예시를 축으로 잡아, 반대 방향이라는
것이 이름만으로 보이게 했습니다. **「예고」라는 말은 쓰지 않습니다** — 앱 부제가
"예시는 예고가 아니라 후보다"라 말이 부딪힙니다.
`미예고율`은 `예시 밖 비율`로 부릅니다(계산식은 그대로).
※ 「헛스윙 분석」 탭은 다른 것을 잽니다(스냅샷끼리의 흔들림). 그 탭 이름은 그대로 둡니다.

**비규격·정규격 비중** — 헛예시·예시 밖을 각각 비규격/정규격으로 갈라 셉니다.
비규격은 치수가 매번 달라 예시가 흔들리는 것이 어느 정도 불가피하지만,
**정규격이 어긋나는 것은 규격이 정해진 물건인데 예측이 빗나간 것**이라 성격이 다릅니다.
같은 7장이어도 정규격 쪽이 크면 훨씬 나쁜 신호입니다.
(2026-09-08 실측: 헛예시 7 = 비규격 7 · 정규격 0 / 예시 밖 9 = 비규격 8 · 정규격 1)

**대조 기준 예시는 「발주일 전날까지」 뽑은 것입니다 (2026-09-09 확정)**

- 출고예시는 '앞으로 나갈 것' 목록이라, **발주가 확정되면 그 물량이 예시에 들어갑니다.**
  그래서 **발주 당일 오후에 뽑은 예시**에는 그날 발주가 이미 다 들어 있습니다.
  그걸로 그날 발주를 맞추면 **100% 일치가 나오지만 아무것도 알 수 없습니다** —
  답을 쓰고 나서 제가 쓴 답을 베껴 놓고 채점하는 것과 같습니다.
  (실측: 09-09 예시로 09-09 발주를 맞추니 86/86 · 예시 밖 0.0%)
- 그래서 `buildMatchRows`는 `buildLatestByShip(orderDate)`를 씁니다.
  `authoritativeSnaps(before)`가 **asof < 발주일** 인 스냅샷 중 최신만 고릅니다.
  화면에도 「기준 예시 2026-09-08」로 어느 날 것을 썼는지 적습니다.
- 이 규칙은 **대조에만** 적용합니다. 2주 물량·발주 예상은 늘 최신 예시를 봅니다
  (`buildLatestByShip()` 인자 없이 호출 — 동작 그대로).
- 발주일보다 앞선 예시가 하나도 없으면 숫자를 지어내지 말고 화면에 그렇게 드러냅니다.
- 실측 대조값: 발주 09-08 → 기준예시 09-07 → 예시 밖 12.7% ·
  발주 09-09 → 기준예시 09-08 → 예시 밖 10.5%.

**올린 납품서는 저장합니다 (2026-09-09)**

- 예전에는 `DELIVS`가 메모리에만 있어 **새로고침 한 번에 통째로 날아갔습니다.**
  스냅샷(`SNAPS`)은 저장되는데 납품서만 빠져 있었습니다. 그러면 지난 발주일을 다시
  들여다볼 수도, 대조를 쌓을 수도 없습니다.
- `ybn-delivs-v1` 에 저장하고, 넣을 때마다 `delivSave()` 를 부릅니다.
  **발주일 기준 최근 120일**만 남기고 오래된 것은 덜어냅니다(`DELIV_KEEP_DAYS`).
- 같은 납품번호는 새것으로 갈아끼우므로 **같은 파일을 몇 번 넣어도 부풀지 않습니다.**
- 「올린 납품서 비우기」는 이제 되돌릴 수 없는 삭제라 **먼저 물어봅니다.**
- 화면에 쌓인 장수·발주일 범위를 늘 적어 둡니다 — 저장되고 있다는 것이 보여야 합니다.
- ⚠ localStorage 읽기는 `try/catch` 로 감쌉니다 (9-3).

**낱말 재정정 (2026-09-09 두 번째)** — 화면 이름은 **`납품서에 없음` · `예시에 없음`** 입니다.

- 「헛스윙」→「헛예시」로 한 번 고쳤으나 **여전히 한 번 더 생각해야 한다**는 지적을 받았습니다.
  비유든 줄임말이든 **머릿속에서 풀어야 하는 이름은 결국 안 읽힙니다.**
- 지금 이름은 **어느 쪽에서 빠졌는지를 그대로 적은 것**입니다. 외울 것이 없습니다.
  둘 다 `～에 없음` 꼴이라 반대 방향이라는 것이 이름만으로 보입니다.

| 이름 | 뜻 | 뒤따르는 일 |
|---|---|---|
| **납품서에 없음** | 예시엔 떴는데 발주가 안 옴 | 만들어 두면 **재고로 남습니다** |
| **예시에 없음** | 예고 없이 발주가 옴 | **급하게 만들어야 했던** 물량입니다 |

- 비율(옛 `미예고율`·`예시 밖 비율`)은 이름을 따로 두지 않고 **「예시에 없음」 옆 괄호**에
  `9 (12.7%)` 꼴로 붙입니다. 지표 이름이 하나 줄어듭니다.
- 낱말은 `MATCH_LABEL` 한 곳에 모여 있습니다. 또 바꾸실 일이 있으면 거기만 고치면 됩니다.

---

### 12-11. 발주는 **앱 발주탭 한 곳**에서만 냅니다 (사용자 확정 2026-09-16)

> **역할 나누기: 비규격 작업지시서 = 치수를 푸는 곳 · 앱 발주탭 = 발주를 내는 곳.**

- **왜 고쳤나** — 두 화면이 **같은 상자(`ybn-order-v1`)** 를 쓰는데 반쪽만 이어져 있었습니다.
  - 앱 창이 열려 있으면 비규격에서 적은 발주가 **덮여 사라질 수 있었습니다.**
    앱은 창을 열 때 읽은 `orderLog` 를 통째로 다시 씁니다(`saveOrderLog`).
    비규격은 앱 메뉴에서 `target="_blank"` 로 열리니 **둘이 같이 열려 있는 게 보통**입니다.
  - 비규격에서 기록한 발주는 **구글시트에 안 올라갔습니다**(`autoSync` 를 안 부름).
  - 종류 목록이 두 벌(`OD_GLASS_CATALOG` vs `OD_PRESETS`)이라 **이미 어긋나 있었습니다** —
    현성유리 3.2T 백사틴 두 줄과 `5T 백유리(유리장식장)` 표기가 앱에만 있었습니다.
- **인계함 `ybn-order-handoff-v1`** — 비규격의 `odHandoffSend()` 가 유리 사이즈를
  **색마다 갈라** 담고, 앱의 `odHandoffBanner()` 가 띠로 보여 줍니다.
  같은 주소라 **`storage` 신호**로 다른 창에 새로고침 없이 뜹니다.
  앱이 받아 가면(`odHandoffTake`) 그 색은 인계함에서 지워집니다.
- **발주서는 색마다 한 장** (사용자 확정) — 에토프는 현성, 나머지는 대원처럼 **거래처가
  갈리기 때문**입니다. 한 발주서에 섞지 않습니다.
  - `odSaveOrder` 가 기록하면서 품목을 비우므로 다음 색은 깨끗한 칸에서 시작합니다.
  - 아직 기록 안 한 다른 색이 품목에 남아 있으면 **말없이 붙이지 않고 여쭙니다.**
- **베일(HPM)은 유리가 아니라** 유리 발주 목록에 애초에 안 들어갑니다. 화면에도 그렇게 적습니다.
- ⚠ **비규격 쪽 발주서 코드(`OD_GLASS_CATALOG` 이하)는 더 이상 쓰지 않습니다.**
  화면에서 단추를 걷어내 닿지 않습니다. 되돌릴 일이 없으면 지웁니다.
  **거기 종류 목록을 고치지 마십시오** — 정본은 앱의 `OD_PRESETS` 입니다.
- 종류 고르는 칸에만 쓰임새를 덧붙이려면 `OD_PRESETS` 에 **`pick:` 한 줄**을 더합니다.
  `type` 은 손대지 않습니다 — 발주 기록·엑셀·비고 기억이 모두 그 이름을 씁니다.

---

### 12-12. 자료가 없는 게 아니라 **다른 표에 있었습니다** (2026-09-16)

> **한 줄 규칙: `MODEL_BOM` 이 비면 `MODELS` 를 봅니다. 둘 다 없을 때만 「모른다」고 합니다.**

`ybn-data.js` 안에 유리 사이즈를 담은 표가 **둘**입니다.

| 표 | 무엇 | 담는 힘 |
|---|---|---|
| `MODEL_BOM` (193줄) | 모델 → 유리 목록 | **합적(유리 여러 장)까지** 담습니다 |
| `MODELS` (166줄) | 모델 기본 정보 | 유리 **한 벌**만 |

- `MODEL_BOM` 에 **WFMD 네 줄**(6035·6048·9035·9048)이 통째로 빠져 있었고, 앱은 그 표만 보고
  있었습니다. 그래서 에토프재고에 「전개 실패 — 유리 사이즈를 못 찾았습니다」,
  「595×351 마스터에 없음」이 떴습니다. **`MODELS` 에는 버젓이 있었습니다.**
- 메운 곳 **두 군데** (둘 다 `ybn-data.js`):
  - `bomOf()` — `MODEL_BOM` 이 없을 때만 `MODELS` 의 `glass_w×glass_h` 로 (`src:'MODELS'` 표시)
  - `SIZE_TO_MODELS` 바로 뒤 — 사이즈→모델 표의 빈자리도 같은 규칙으로
- **`MODEL_BOM` 이 있으면 언제나 그쪽이 이깁니다.** `MODELS` 는 한 벌뿐이라 합적을 못 담습니다.
- **둘 다 없으면 예전처럼 화면에 드러냅니다.** 지어내지 않습니다.
- ⚠ **사이즈를 `MODEL_BOM` 에 옮겨 적지 마십시오** — 두 곳에 두면 어긋납니다(규칙 1).
- 근거(2026-09-16 전수): 두 표에 다 있는 **유리 한 장짜리 148줄이 전부 일치**(어긋난 줄 0).
  형제 코드 `WRMBS 9035/9048` 이 `895×351`·`895×479` 로 WFMD 값과 같습니다.
- **월평균(`ETP_SIZE_MASTER`)은 이것과 별개입니다.** 26년 1~8월에 안 나간 사이즈는 여전히
  월평균이 없고 「확인 필요」로 남습니다. 실적이 0장이면 화면이 그렇게 적습니다.

### 12-13. 좌우(L/R)는 **품명 끝**에 붙어 옵니다 (2026-09-16)

- 발주서를 올릴 때 「관통보링인데 좌우(L/R)가 없습니다」로 줄이 빠졌습니다.
  품명에는 `Euro700 Grace ETP OB WTARG 60154 L` 로 적혀 있는데 **규격 칸은 `592*1538` 뿐**이라,
  규격 칸만 보던 `etpNormSize` 가 못 읽었습니다.
- 이제 `etpNormSize(raw, lrHint)` 로 힌트를 받고, `etpLrFromName(품명)` 이 끝의 L/R 을 읽습니다.
  **규격 칸에 적힌 것이 언제나 먼저입니다.**
- ⚠ **관통보링 사이즈일 때만 붙입니다.** 좌우를 가르는 것은 관통보링뿐이고(인쇄면이 있어 서로
  바꿔 쓸 수 없음), 나머지는 좌우가 같은 유리라 사이즈로 합쳐 셉니다.
  안 막으면 `HD B 2091 L` 같은 흔한 품명 탓에 `195x761` 과 `195x761 L` 로 **재고가 두 칸으로 쪼개집니다.**
- 물린 곳 셋: 발주서 올리기(엑셀) · 발주서 올리기(PDF·글자) · 발주탭→에토프 연동(`etpPoFromOrder`).

### 12-14. 딸림파일 판 검사는 **「적어도 이 판」** 입니다 (2026-09-18)

- `비규격작업지시서.html` 의 `PARTS_NEED` 는 `v`(딱 맞기)가 아니라 **`vMin`**(적어도 이 판)입니다.
- **왜**: 예전에는 `ybn-data.js` 판을 올릴 때마다 여기도 같이 올려야 했고, 잊으면 **멀쩡한 새
  파일을 「낡은 판입니다」라고 알리는 헛경보**가 났습니다. 두 번 겪었습니다(09-14 · 09-16).
  숫자를 두 곳에 두면 반드시 어긋납니다(규칙 1).
- 딸린 파일은 값이 **더해지기만** 하므로 판이 높은 것은 탈이 없습니다. **낮을 때만** 알립니다 —
  그게 진짜 '캐시에 낡은 파일이 남은' 경우입니다.
- **`vMin` 은 그 화면이 실제로 그 판의 값을 써야 할 때만 올리십시오.**

### 12-15. BD2R 마이다 — 납품 세로가 910이 아니면 **상부만** 늘고 준다 (사용자 확정 2026-09-18)

> **한 줄 규칙: 하부는 350 고정, 상부가 나머지를 다 받습니다. 범위는 880~940 뿐입니다.**

- **근거가 두 겹입니다.** 사장님 말씀(2026-09-18)과, 캐드 원본 도면(`DOOR_LIB` 의 BD2R **OB**)에
  적힌 메모가 같은 말을 합니다 — 「높이 장: 880~940 / 도어: 상부길이 = 변경(320~380) X값 변환 /
  하부길이 = 고정(350)」.

| 납품 세로 | 상마이다 | 하마이다 | 유리 |
|---|---|---|---|
| 880 | **320** | 350 | 각 `0 × −3` |
| 910 (기준) | 350 | 350 | 〃 |
| 940 | **380** | 350 | 〃 |

- `상마이다 = 350 + (납품세로 − 910)`. 가로는 −5 그대로(납품 900 → 895).
- **880 미만·940 초과는 규칙을 못 받았습니다** (사장님 2026-09-18 "그 이하나 그 이상은 또 확인해봐야 해")
  → 계산하지 말고 '확인 필요'로 드러냅니다. ([`확인대기.md`](확인대기.md) B13)
- **HD 는 캐드 도면에 이 메모가 아예 없습니다.** 910 말고는 전부 '확인 필요'입니다
  (사장님 2026-09-18 "그때 되서 알아볼게"). ([`확인대기.md`](확인대기.md) B12)
- **EURO500 도 답을 못 받아 '확인 필요'** 로 둡니다. 도면 메모는 EURO700 OB 것뿐입니다. (B14)
- **BD3U 도 같은 성격입니다** — 도면 머리글로 **범위 880~940 은 확인**했으나(사장님 제공
  옆면 도면 2026-09-18) 조각 나누는 규칙이 아직입니다. 지금은 910 말고는 전부 '확인 필요'. (B11)

**숫자는 `SPLIT_VARY` 표 한 곳에만 둡니다.** 새 코드는 계산 코드를 고치지 말고 **표에 한 줄**만 더합니다.

```js
const SPLIT_VARY={
  BD2R:{ base:910, lo:880, hi:940, types:['OB'], vary:'상마이다', fixed:'하마이다' },
  BD3U:{ base:910, lo:880, hi:940, types:[],     vary:'소마이다', fixed:'대마이다' },
};                          // types 가 비면 = 그 계열은 전부 '확인 필요'
```

**도면은 앱이 직접 그립니다** (`splitVaryDoorSVG`). 캐드 라이브러리 도면은 910 한 가지뿐이라
880에 붙이면 **틀린 그림**이 됩니다. 상·하를 **함께** 그립니다(사용자 확정 — 상부만 바꿔 넣으면
하부 그림이 종이에서 사라집니다).

- 단면은 캐드 원본 그대로 — 바깥날 1.4 · 위아래 레일 15 · 좌우 선대 45.
- **PVC 인서트(초록)** 는 좌·우 양쪽, 바깥날에서 **28.5mm 안쪽**. 밑에서부터
  **상마이다 68 / 32 / 64** · **하마이다 68 / 32 / 128**. 나머지가 맨 위 칸이라
  길이가 바뀌면 **거기만** 늘고 줍니다(880 → 156). 캐드 원본의 초록 자리를 실측해 옮겼습니다.
- ⚠ **`cadDrawKey` 에 세로를 넣어 910과 880을 갈랐습니다.** 안 가르면 '그림이 같으면 한 덩어리'
  규칙(12-8-2)에 걸려 **350짜리 그림 하나가 880 물량에도 붙습니다.**
- ⚠ 앱이 그린 도면에는 `data-gen="1"` 이 붙고 `libDoorDimTextOnLine` 이 건너뜁니다.
  그 함수는 **캐드에서 들여온 도면**을 바로잡는 것이라, 앱 도면에 또 걸면 두 번 눕습니다.

**합적 줄 모양 — 기본 틀은 한 줄, 갈린 쌍은 `상A·하A·상B·하B`** (사용자 확정 2026-09-18)

> 하루에 네 번 손댄 자리입니다. **이것이 마지막입니다.** 가름의 뜻:
> **910 은 늘 나오는 기본 틀이라 한 줄이면 알고, 910 이 아닌 것은 아주 가끔 나와 헷갈리므로**
> **상세히** 적습니다(사장님 말씀 그대로).

```
BD2R 910 :   895×350 = 1SET(2EA)      ← 크기가 같다 → 한 줄
               895×347
BD2R 880 :   상A - 345×320 = 1EA      ← 크기가 갈린다 → 줄머리
                 345×317
             하A - 345×350 = 1EA                          웜코튼
                 345×347
             상B - 895×320 = 1EA
                 895×317
             하B - 895×350 = 1EA
                 895×347
```

- **크기가 같은 조각은 한 줄로 합칩니다.** 합친 줄에 `1SET(2EA)` 를 적습니다.
- **크기가 갈리면 줄이 둘 이상**이 되고 줄머리 `상 -`·`하 -`(`SEG_LAB`)가 붙습니다.
  소·대마이다는 `소 -`·`대 -`. **줄머리는 코드·모델명·조각 이름 모두 검정**입니다(12-19).
- **세트는 줄머리 글자로 묶습니다**(`CAD_SET_LETTER`) — `상A · 하A · 상B · 하B`.
  사장님 2026-09-18 "왼쪽에 상A 하A 상B 하B 이런식으로 차라리 세트로 만들고
  우측 세트 꺾쇠는 없애줘". **오른쪽 세트 꺾쇠는 안 그립니다**(`CAD_SET_BRACKET=false`).
  꺾쇠가 하나 줄어 오른쪽이 넓어지고, 어느 상과 어느 하가 한 벌인지가 줄만 봐도 보입니다.
- ⚠ **글자는 갈린 쌍이 둘 이상일 때만** 붙입니다. 한 쌍뿐이면 `상 -`·`하 -` —
  짝이 하나인데 A 만 있으면 없는 B 를 찾게 됩니다. 차례는 **덩어리 전체에서 이어 셉니다**(색이 달라도).
- ⚠ **줄머리는 안 합쳐졌을 때만** 붙입니다. 서로 다른 조각이 한 줄로 합쳐지면 그 줄이
  상인지 하인지 가릴 수 없습니다(`o.label=null`). 같은 조각끼리 합쳐진 것
  (BD3U 소마이다 2장)은 줄머리를 그대로 답니다.
- 제목 옆·색이름 뒤의 `×2SET` 은 **안 적습니다** — 줄머리 글자가 이미 말합니다.
- 꺾쇠와 수량 글자 사이 틈은 **`CAD_BRACKET_GAP`(8mm) 한 곳**에서 정합니다.
- ⚠ 꺾쇠는 내용 박스(201.25mm)를 못 넘습니다 — `BX_SET` 에 상한이 있습니다.
  **치수줄 문구나 줄머리를 고치면 오른쪽 끝을 굴림으로 다시 재십시오.**

**도면은 정해진 가로로 그립니다 — 실제 비율이 아닙니다** (`SPLIT_PVC.drawW`, 사용자 확정 2026-09-18)

- 캐드 원본이 345 짜리든 895 짜리든 **똑같이 595 로** 그립니다. 라이브러리 도면 16종도 다 고정입니다.
- 그래서 **도면 안에는 조각 이름만 적고 치수는 안 적습니다.** 한 덩어리에 345 와 895 가
  같이 들어오므로(2026-09-18 이 그랬습니다) 어느 한쪽을 적으면 나머지가 틀립니다.
- **조각 이름(`상마이다`·`하마이다`)은 도어 안 정가운데**에 적습니다 (사장님 2026-09-18
  "애매하게 하지 말고 차라리 도면 안으로 넣어서"). 예전에는 도어 위에 얹어 윗선에 걸쳤습니다.
- **왼쪽 세로 치수는 안 그립니다** (사장님 2026-09-18 "가독성이 너무 떨어져, 왼쪽치수는 빼줘").
  종이에서 도면 칸이 30mm 남짓이라 가운데 32 칸이 1.4mm 밖에 안 됩니다 — 숫자가 제 칸보다 큽니다.
  PVC 자리는 초록 표시로만 알립니다.

### 12-16. BLD1UA · BNDC2N · BNDB2N — 원본 DXF 에서 뽑아 넣었습니다 (2026-09-18·22)

> 출처: 사장님이 주신 **`클로드도면BLD.BNC.dxf`** (그날 캐드로 만드신 작업지시서).
> 캡처가 아니라 **DXF 좌표를 실측해** 옮겼습니다. 눈대중한 값이 없습니다.

| 코드 | 도면 | 표시 | 자리 |
|---|---|---|---|
| **BLD1UA** | 595 × 250 | **ø20 초록 2개** (블록 `28-B`) | 밑에서 **100** · 양 옆 바깥날에서 **137.5** |
| **BNDC2N** | 295 × 732 | **ø3 검정 4개** (블록 `B-2`) | 가운데서 **±45** · 밑에서 **165 · 485** |

- 단면은 BD2R 과 **똑같습니다** — 바깥날 1.4 · 위아래 레일 15 · 좌우 선대 45 · 모서리 눈금 2.5.
  원본에서 재 보니 세 도면이 전부 같은 값이었습니다.
- 표시 색은 **원본 레이어 그대로**입니다. BLD1UA 의 INSERT 는 `HIDDEN`(초록),
  BNDC2N 은 `0`(검정)에 놓여 있습니다. 십자선은 둘 다 `CENTER`(청록).
- 도면 크기는 **고정**입니다(라이브러리 16종과 같은 방식). 445 짜리도 595 짜리도 같은 그림입니다.
- ✅ **BNDB2N 도면도 넣었습니다** (원본 보링도 DXF, 2026-09-22). 도어 **195 × 732** ·
  **ø3 검정 4개** · 가운데서 **±45** · 밑에서 **165 · 485**.
  세로도 보링 자리도 BNDC2N 과 **똑같고, 다른 것은 양옆 여백뿐**입니다
  (BNDC2N `102.5/90/102.5` · BNDB2N `52.5/90/52.5`).
  ⚠ **그래서 `DOOR_ALIAS` 로 BNDC2N 도면을 돌려쓰면 안 됩니다** — 여백 치수가 틀리게 찍힙니다.
- ✅ **BD3U 도면은 이미 맞았습니다** — 2026-09-22 보링도 DXF 로 대조만 했습니다.
  라이브러리의 BD3U 는 조각 **173 · 173 · 350**, 보링이 각각 **96.5/76.5** · **86.5/86.5** ·
  **246/104**, ø20 간격 **320** 으로 원본과 전부 맞습니다. **더할 것이 없습니다.**
  (확인대기 B11 의 「86.5·76.5 가 무엇을 잰 값인가」는 여기서 풀립니다 — **조각 높이가 아니라
  그 조각 안의 보링 자리**입니다. 두 소마이다는 원본에서도 둘 다 173 입니다.)
- **`BLD1UA` 를 `RULES` 에 넣었습니다.** 그전에는 아예 없어 **계산조차 안 되고** '미지 코드'
  목록에만 떴습니다. 힌지 없는 마이다이고 **합적이 아닙니다**(1EA).
  유리 `0 × −3` 은 원본으로 확인 — `445×220 → 445×217` · `595×220 → 595×217`.
- ⚠ **캡처와 DXF 가 어긋난 자리가 있습니다.** 사장님 캡처에는 `295×702` 가 **BLD1UA** 로,
  DXF 에는 같은 크기가 **BNDC2N** 으로 적혀 있습니다. DXF 를 따랐습니다.
  → [`확인대기.md`](확인대기.md) **B16**.
- 도면을 더 넣을 때도 **DXF 를 받아 좌표를 재십시오.** 캡처로는 mm 를 못 읽습니다.

**DXF 에서 도면을 뽑는 법** (다음에 되풀이할 때)

1. `ENTITIES` 구간을 잘라 냅니다 — `sed -n '<시작>,<끝>p'`. **머리글 `ENTITIES` 줄은 빼십시오.**
   안 빼면 코드·값 짝이 한 칸 밀려 아무것도 안 읽힙니다.
2. DXF 는 **코드 줄 / 값 줄이 번갈아** 옵니다. 그 짝으로 훑으면 `LINE`(10·20→11·21) ·
   `CIRCLE`(10·20·40) · `MTEXT`(1·3 이어붙이기) · `INSERT`(2=블록이름, 8=레이어)를 뽑을 수 있습니다.
3. **글자는 이미 UTF-8 입니다.** 머리글의 `$DWGCODEPAGE ANSI_949` 를 믿고 `iconv` 를 걸면 깨집니다.
4. `INSERT` 는 **제 레이어의 색을 블록 안 레이어 `0` 짜리 도형에 물려 줍니다.** 색을 정할 때 봐야 합니다.

### 12-17. BNDC2N·BNDB2N 세로는 **−178** · BLD1UA 는 **−660** (원본 DXF 실측, 2026-09-18·22)

> 사장님 말씀: "BNC2N도 910 기준으로 910일때 732로 계산해줘야 해."

```
프레임 세로 = 납품 세로 − 178        유리 = 프레임 −0 × −3
  910 → 732   (사장님 말씀)
  880 → 702   (사장님 캐드 원본과 같음 — 유리 699)
```

- 그전에는 **−4** 로 풀어 880 을 **876** 으로 냈습니다. **174mm 틀린 값이 종이로 나가고 있었습니다.**
- 코드 자리는 `RULES.BNDC2N` 의 **`doorHsub:178`** 한 줄입니다. B(146)·BAD(324)와 같은 방식이라
  `calc()` 는 안 고쳤습니다.
- 도면(295×732)은 **910 기준 프레임**이라 그대로 맞습니다.
- ✅ **형제 코드 `BNDB2N` 도 −178 로 같습니다** (원본 보링도 DXF, 2026-09-22).
  `23 Euro700 Grace 3종 OB - BNDB2N 보링도.dxf` 를 실측했습니다 — 도어 **195 × 732**.
  세로 732 가 BNDC2N 과 **똑같고**, 가로만 195(납품 200 −5)로 좁습니다.
  보링 자리도 같습니다 — 가운데서 **±45** · 밑에서 **165 · 485**.
  다른 것은 **양옆 여백뿐**입니다: BNDC2N `102.5 / 90 / 102.5`(=295) · BNDB2N `52.5 / 90 / 52.5`(=195).
  → `RULES.BNDB2N` 에 `doorHsub:178`. ([`확인대기.md`](확인대기.md) B17 닫힘)
- ✅ **`BLD1UA` 세로는 −660 입니다** (원본 보링도 DXF, 2026-09-22).
  `18 Euro700 Grace 3종 OB - BLD1UA 보링도.dxf` 실측 — 도어 **595 × 250**.
  납품 600×910 기준이므로 가로 −5 · **세로 −660** 입니다.
  **도면 두 장이 서로를 확인해 줍니다** — 09-18 원본의 `445×220`(납품 450×880)도
  `880 − 660 = 220` 으로 딱 맞습니다. 유리는 그대로 `0 × −3`(220 → 217).
  → `RULES.BLD1UA` 에 `doorHsub:660`. ([`확인대기.md`](확인대기.md) B18 닫힘)
- ⚠ **그전에는 앱이 납품 880 을 `445×876` 으로 냈습니다 — 656mm 틀린 값입니다.**
  규칙이 빠진 코드가 조용히 −4 로 흐르면 이렇게 됩니다. 새 코드는 반드시 원본과 맞춰 보십시오.

**같은 함정을 어떻게 찾았나** — 사장님이 손으로 만드신 캐드 작업지시서(DXF)와 **앱 출력을 나란히**
놓고 숫자를 맞춰 봤습니다. 규칙이 빠진 코드는 **조용히 −4 로 흘러** 아무 경고 없이 틀린 값이 나갑니다.
**새 코드를 넣을 때는 반드시 원본과 한 번 맞춰 보십시오.**

### 12-18. BAD — 속이 **허니컴이 아니라 MDF** 입니다 (원본 보링도 DXF, 2026-09-21)

> 출처: 사장님이 주신 **`12 Euro700 Grace 3종 OB - BAD 보링도.dxf`** (한샘 제작도면 A3).
> 좌표를 실측해 옮겼습니다 — **눈대중한 값이 없습니다.**

**⚠ 먼저 바로잡습니다 — BAD 는 MDF 통판이 아닙니다.** 처음에는 그렇게 적었으나 원본 비고란이
이렇게 말합니다. **알루미늄 프레임 도어인데 속(심재)만 MDF** 입니다.

```
-프레임 일반 : 19t X 45mm AL프레임      -손잡이 : 19t X 15mm AL프레임
-심  재 : 9t MDF(E0,35형)              ※ 심재 : 9t MDF 사양(허니컴 사양 아님)
-유  리 : 100% 완전강화 3.2t 새틴백유리 + 후면 컬러실크인쇄
-뒷  판 : 2.7t MDF                     -마감 : 아노다이징 피막(실버샴페인)
```

**그래서 종이에 `MDF` 를 적어 둬야 합니다** — 안 적으면 허니컴으로 만들게 됩니다.
(사장님 2026-09-21 "mdf라고 적어줄래. 그때 상마이다 하마이다 적은거처럼 도면 중간에")

| | 값 |
|---|---|
| 도어 | **595 × 586** (모서리 R0.5) |
| 단면 | 바깥날 1.4 · 위아래 레일 15 · 좌우 선대 45 · 모서리 눈금 2.5 **양쪽** |
| 보링·힌지 | **없음** — 원본 도면에 표시가 하나도 없습니다 |

- 단면이 BD2R·BLD1UA·BNDC2N 과 **전부 같습니다**(1.4 / 15 / 45). 네 도면에서 다 같은 값이었습니다.
- ✅ **세로 −324 는 비규격에서도 같습니다** (사장님 확정 2026-09-21). 상부고정부가 크기와
  상관없이 늘 324 를 먹습니다. 앱 메모에 「세로차감 큼 확인」 이라고 걸려 있던 표시를 지웠습니다 —
  걸어 둔 지 오래됐는데 확인이 안 된 채였습니다.
  근거 두 겹: **정규격**은 원본 보링도로 대조(910 → 586) · **비규격**은 사장님 확정.
- 도면의 **595×586 이 앱 계산과 정확히 맞습니다** (`납품 600×910 → −5 × −324`).
  BNDC2N 처럼 어긋난 데가 없습니다. **치수 규칙은 안 고쳤습니다.**
- 모서리 눈금이 **양쪽**인 것만 다른 도면과 다릅니다(다른 것들은 왼쪽만).

**`centerTxt` — 도어 한가운데 글자는 `RULES` 한 곳에서 나옵니다**

- `RULES.BAD.centerTxt='MDF'` 하나가 **캐드 도면(`libDoorFilled`)과 파라메트릭 도면(`doorSVG`)
  양쪽**에 얹힙니다. 도면 SVG 에 글자를 박아 두면 두 곳이 되어 어긋납니다(규칙 1).
- 새 코드에 글자를 넣으려면 **`RULES` 에 한 줄**만 더하십시오. 그리는 코드는 안 고칩니다.
- ⚠ 자리는 **viewBox 한가운데**입니다. 앱이 그린 도면은 도어를 가운데에 놓으므로 맞습니다.
  **캐드에서 통째로 들여온 도면은 도어가 가운데가 아닐 수 있으니** 그런 코드에 쓸 때는 보고 확인하십시오.
- 화면 안내 문구도 여기서 나옵니다 — **「힌지 없음 · 심재 MDF(허니컴 아님)」**.
  예전에는 힌지 없는 코드를 전부 「마이다(서랍) · 합적」 이라 적었는데 BAD 는 서랍도 합적도 아닙니다.
  다른 힌지 없는 코드(BADMIL · BLD1R · BLD1UA · BDI1R · BLD2R)는 **예전 그대로**입니다.
- ✅ **캐드 도면은 고정 가로 595 그대로 둡니다** (사장님 확정 2026-09-22).
  BAD 는 비규격 세로가 376~876 까지 벌어져 그림이 실물 느낌과 달라 보이는데,
  **바깥 네모만 실제 치수대로** 늘렸다 줄였다 할 수 있는지 여쭈었습니다.
  **지금처럼 고정으로 둡니다** — 치수는 종이에 제대로 적히니 재단이 틀어질 일이 없습니다.
  (`boxFromSize` 는 아래 대로 **캐드 도면이 못 붙었을 때만** 씁니다.)
- `boxFromSize:true` 는 **캐드 도면이 못 붙었을 때의 대비**입니다(`doorSVG` 가 네모를 실제
  프레임 비율로 그립니다). 고정 비율(78×118)로 두면 세로로 길쭉해 보입니다.
- **손잡이(주황)는 파라메트릭 도면에만 남아 있습니다.** 캐드 도면에는 원본대로 없습니다. (B19)

**이 DXF 에서 더 배운 것** (12-16 의 뽑는 법에 덧붙임)

- `TEXT`(한 줄 글자)와 `TRACE`(네 점 채움)도 나옵니다 — `MTEXT` 만 읽으면 **제목란·비고란을 통째로 놓칩니다.**
  BAD 는 글자가 62개인데 **`MTEXT` 는 3개뿐**이었습니다.
- **비고란을 반드시 읽으십시오.** 「심재 9t MDF(허니컴 사양 아님)」 처럼 **치수에는 안 나오는데
  생산에 결정적인** 내용이 거기 있습니다.
- `DIMENSION` 의 실제 숫자는 **딸린 `*D*` 블록 안 `MTEXT`** 에 있습니다(`595` · `586` · `4-R0.5`).
  엔티티만 보면 `STANDARD` 라는 스타일 이름만 나옵니다.

### 12-19. 약어는 **검정** — 색으로 뜻을 나누는 것은 빨강 하나뿐 (사용자 요청 2026-09-22)

> 사장님 말씀: "약어들 파란색으로 폰트가 지정되어있는데 검정색으로 바꿔줄래? BD2R, M, 등등 이런것들"

**바꾼 곳 넷 — 전부 「약어(코드)를 적는 자리」입니다.**

| 어디 | 무엇 |
|---|---|
| 캐드 양식 **제목줄** | `BD2R` · `M` · `BLD1UA` … 블록 맨 윗줄 |
| 캐드 양식 **줄머리** | 코드·모델명(`WFUP` · `M 3073`). 조각 이름(`상 -`)은 이미 검정이었습니다 |
| 옛 작업지시서 `.code-tag` | 30px 큰 약어 |
| 프레임(조립1) 탭 코드 칸 | 표 안 약어 |

- 이제 **줄머리는 코드·모델명·조각 이름이 모두 검정**입니다. 예전에는 코드·모델명만 파랑이었는데,
  제목줄을 검정으로 바꾸면서 같이 맞췄습니다 — **한 덩어리 안에서 같은 것(약어)이 자리에 따라
  색이 달라지면** 오히려 헷갈립니다.
- ✅ **폴백 도면의 힌지도 검정으로 바꿨습니다** (사장님 확정 2026-09-22, 같은 날 두 번째).
  파라메트릭 폴백 도면(`doorSVG`)의 **힌지 표시와 간격·변환 숫자**가 마지막 파랑이었습니다.
  **힌지 동그라미 `#333`**(도어 테두리와 같은 선 색) · **숫자 `#111`**(약어 `.code-tag` 와 같은 글자 색).
  선 일감은 `#333`, 글자는 `#111` — 이 파일이 원래 쓰던 가름을 그대로 따랐습니다.
- **이로써 종이에 남는 색은 빨강(재단치수·변환 강조)과 주황(손잡이) 뿐입니다.**
  단추·칩·띠·날짜 머리글 같은 **화면 장식의 파랑은 그대로**입니다 — 종이에 안 찍힙니다.
- **뜻을 나르는 색은 빨강 하나로 모읍니다.** 종이에 색이 둘 이상이면 무엇이
  급한지 흐려집니다. 새 글자를 넣을 때 **파랑을 다시 쓰지 마십시오.**

### 12-20. 앱에서 가져온 행은 **좌우(L/R)를 품명에서 다시 읽습니다** (2026-09-22)

> 사장님 지적: "수납도어 납품서에 좌우 구분되어있는데 왜 못고쳐."
> 제가 **확인도 안 하고 못 한다고 말한 것**입니다. 납품서를 열어 보니 좌우가 버젓이 있었습니다.

**무엇이 잘못됐나** — 납품서를 읽는 길이 **둘**인데, 한쪽만 좌우를 챙기고 있었습니다.

| 길 | 좌우 |
|---|---|
| PDF·엑셀을 이 화면에 직접 올리기 | `delivSizeLR()` 이 읽습니다 ✅ |
| **앱에서 「가져오기」**(`ybnDocToRows`) | `it.lr` 만 보고 **그 함수를 아예 안 불렀습니다** ❌ |

앱이 좌우를 안 담아 준 품목은 그대로 빈 채 넘어와, 수납도어 키큰장이 `R1` 이 아니라
**`1 EA`** 로 찍혔습니다. 이제 **비었을 때만** 품명에서 다시 읽습니다
(`it.lr` 이 있으면 **언제나 그쪽이 먼저**입니다).

**왜 키큰장만 그랬나 — 품명이 칸 너비에 잘려 두 조각이었습니다**

실제 납품서(2026-09-22)를 풀어 보니 이렇습니다.

```
… 비규격(600*0*2255*0        ← 앞조각. 닫는 괄호가 없습니다
R)  600X0X2255X0             ← 뒷조각. 괄호와 R 이 여기 맨 앞에 있습니다
```

벽장(`비규격(450*0*736*0 L)`)은 짧아 안 잘리니 멀쩡했습니다. `delivSizeLR` 은 이런 꼴도
읽게 되어 있습니다(홀로 선 `L)`·`R)` 규칙 — 12-6·12-13) — **부르기만 하면 됐습니다.**

- ✅ 검증(2026-09-22 납품서 여섯 품목 전수): 벽장 `445×732 = L2/R2` · `495×860 = R1` ·
  **키큰장 `595×2251` · 유리 `571×2227` · `R1`** — 종이에 찍힌 값·손으로 쓰신 R 과 모두 일치.

**두 번째 구멍 — 직접 올리는 길도 잘린 조각을 안 이어 붙였습니다 (2026-09-22 두 번째)**

> 사장님 지적: "유리장식키큰장 오늘꺼 LR 구분이 안되어있는데."
> 위 고침은 **앱에서 가져오기** 길만 막았습니다. **PDF·엑셀을 직접 올리는 길**에 같은 구멍이 남아 있었습니다.

`parseDelivText` 는 뒷줄을 이어 붙이는 일을 **「이름이 없거나 사이즈가 없을 때」만** 합니다.
그런데 키큰장 줄에는 `비규격` 글자가 있어 **`hasSize` 가 참**이라 그 갈래를 건너뜁니다.

```
ME1TMX001유리장식키큰장 AL도어 비규격(600*0*2255*0     ← 여기서 잘리고
R)  600X0X2255X0 1                                     ← 괄호와 R 이 뒷줄로
```

그래서 **사이즈는 맞는데 좌우만 빠집니다.** 앞조각만으로도 `비규격\(([\d*]+)` 되받이가
`600×2255` 를 읽어 내기 때문에 **아무 경고 없이** 키큰장이 `R1` 이 아니라 `1 EA` 로 나갔습니다.
벽장(`비규격(450*0*736*0 L)`)은 짧아 안 잘리니 멀쩡했습니다 — **그래서 더 안 보였습니다.**

- 고친 곳: `parseDelivText` — `extra` 이어붙이기 **바깥**에 한 갈래를 더 둡니다.
  **괄호가 안 닫혔을 때만**(`/\([^)]*$/`), **다음 줄이 `L)`·`R)` 로 시작할 때만** 이어 붙입니다.
  조건을 좁게 두어 **옆 품목의 값을 끌어올 수 없게** 했습니다.
- ⚠ **`extra` 갈래 안에 넣으면 안 됩니다.** 그 갈래가 바로 키큰장에서 안 도는 갈래입니다.
  실제로 한 번 거기 넣었다가 밖으로 뺐습니다.
- 검증(실제 납품서 `CrystalReportViewer (44).pdf` 네 품목): 벽장 `L`·`R`·`R` 은 고치기 전후가 같고,
  키큰장만 **(없음) → `R`** 로 바뀝니다.

**PDF 를 직접 뜯어 읽는 법** (node·python 없이 — 2026-09-22에 만든 길)

납품서가 무엇을 담고 있는지 **짐작하지 말고 열어 보십시오.** 오늘 이것으로 원인을 못 박았습니다.

1. **스트림 풀기** — PowerShell 의 `System.IO.Compression.DeflateStream`.
   PDF 스트림은 zlib 이라 **앞 2바이트(머리표)를 건너뛰고** raw deflate 로 풉니다.
   바이트를 Latin1(`GetEncoding(28591)`)로 읽으면 1바이트 = 1글자라 자리 찾기가 쉽습니다.
2. **글자는 폰트 코드입니다** — 그대로는 못 읽습니다. 폰트마다 딸린 **ToUnicode 표**
   (`beginbfrange`)로 돌려야 합니다. 어느 폰트가 어느 표인지는 폰트 객체의 `/ToUnicode N 0 R`.
   이 납품서는 `/a` = 영문·숫자 · `/9` = 한글 · `/e`·`/f`·`/g` = 쪽번호·바코드 였습니다.
3. **읽는 차례대로 뽑으십시오.** `Td`·`Tm` 의 y 로 줄을 묶으면 어긋납니다 —
   `Td` 는 **직전 줄에 견준 값**이라 절대 위치가 아닙니다. 조각을 순서대로 늘어놓는 편이 정확합니다.

### 12-21. 발주처 기본 납기 — 대원유리 **+1영업일** · 현성유리 **+2영업일** (사장님 확정 2026-09-22)

> 사장님 말씀: "발주일 기준으로 대원유리 +1일 현성유리 +2일 너가 계산한 납기일이 맞아."
> **영업일 기준이 맞다고 확정받았습니다** — 달력일이 아닙니다.

- **기준점은 발주일**입니다(납품요청일이 아닙니다 — 12-7 의 납기와 다른 이야기입니다).
- 주말·공휴일은 `_addBizDays`(`KR_HOLIDAYS`)가 건너뜁니다. 직접 날짜를 세지 마십시오(3번).

| 발주일 | 대원유리 (+1) | 현성유리 (+2) |
|---|---|---|
| 09-22 (화) | 9/23 | **9/28** ← 추석 24~26 건너뜀 |
| 09-23 (수) | 9/28 | 9/29 |
| 10-02 (금) | 10/5 | 10/6 ← 개천절 |

- **숫자는 `index.html` 의 `OD_VENDOR_DUE` 한 곳에만** 둡니다. 발주처를 더하려면 한 줄만 더합니다.
  ```js
  const OD_VENDOR_DUE = { '대원유리': 1, '현성유리': 2 };   // 발주일에서 영업일 며칠 뒤
  ```
- ⚠ **손으로 적으신 값은 덮어쓰지 않습니다.** `od-due` 칸에 글자를 넣는 순간
  `dataset.auto` 표시가 지워져, 「긴급」처럼 적어 두신 것은 **발주일을 바꿔도 살아남습니다.**
  우리가 넣어 둔 값일 때만 다시 씁니다(`odFillDue`).
- 부르는 자리 셋 — 화면 열 때(`renderOrderPage`) · 발주처 바꿀 때(`odVendorChanged`) ·
  발주일 바꿀 때(`od-date` 의 `onchange`). **한 곳이라도 빠지면 칸이 묵은 값으로 남습니다.**
- 기본 납기가 없는 발주처는 예전처럼 빈 칸입니다.

**곁들여 — 종류 기본값은 `def:true` 한 줄입니다.** 대원유리는 `3.2T 백사틴 비규격`이
기본으로 골라집니다(사장님 2026-09-22 "그걸 제일 많이 쓰는데 매번 클릭하려니 귀찮아서").
**목록 차례는 건드리지 않습니다** — 고르는 값만 바뀝니다. 다른 발주처도 `def:true` 한 줄이면 됩니다.

### 12-22. 목대 사양 — **상부마이다가 목대**입니다 (원본 보링도 DXF 실측, 2026-09-22)

> 출처: 사장님이 주신 보링도 **3장** — `16 … BLD1R` · `18 … BLD1UA` · `19 … BLD2R`.
> 좌표를 실측해 옮겼습니다. **눈대중한 값이 없습니다.**

**도면이 스스로 말합니다.** 왼쪽 이름표가 조각마다 어느 사양인지 적어 둡니다.

```
■ 상부마이다  →  18t 목대사양      ← 이것이 목대입니다
■ 하부마이다  →  알판도어 사양      ← 유리도어
```

| 모델 | 유리도어(하부마이다) | **목대(상부마이다)** | 개수 |
|---|---|---|---|
| BLD1R 4591 | 445 × 250 | **405 × 60** | 1 |
| BLD1R 6091 | 595 × 250 | **555 × 60** | 1 |
| BLD1UA 4591 | 445 × 250 | **405 × 60** | 1 |
| BLD1UA 6091 | 595 × 250 | **555 × 60** | 1 |
| BLD2R 6062 | 595 × 350 | **555 × 80** | **2** |

- **목대 가로 = 도어 가로 − 40** 입니다(445→405 · 595→555). 세로는 도면 표에 칸이 없어 고정입니다.
- **개수는 도면 수량란 그대로**입니다 — `상부1,하부1` · BLD2R 만 `상부2,하부1`.
  앱에 이미 들어 있던 `mokdae:2` 와 맞습니다(회귀 없음).
- **재질**: 목대 코어는 셋 다 `18t PB(E0,13형)` → **`MOKDAE_CORE` 한 곳**에 둡니다.
- ⚠ **유리도어의 심재는 목대와 다른 이야기이고 모델마다 갈립니다** —
  BLD1R·BLD2R 은 `11t 허니컴보드`, **BLD1UA 는 `9t MDF(E0,35형)`**.
  BLD1UA 도면에는 `※ 심재 : 9t MDF 사양(허니컴 사양 아님)` 이라고 못 박혀 있습니다(BAD 와 같은 주의, 12-18).
- 숫자 자리는 **`OLD_STD_SIZE` 의 `mokdaeW`·`mokdaeH`·`mokdae` 한 곳**뿐입니다.
  `ybn-data.js` 주석에 `555×57` 이라 적힌 어림값이 있었는데 **실측은 60** 이라 고쳤습니다.
  ⚠ **거기에 숫자를 다시 적지 마십시오** — 두 곳에 두면 어긋납니다(규칙 1).
- 사이즈를 모르는 모델은 예전처럼 **'사이즈 확인 필요'** 로 드러냅니다. 지어내지 않습니다.
- ✅ 검증(2026-09-22): 다섯 모델 모두 `목대 405×60 1개` … `555×80 2개` 로 나오고,
  목대 없는 코드(BAD)는 줄이 아예 안 붙습니다.

**목대 보링 — `ø5 × 12.5` 장부볼트보링 3개** (같은 도면에서 실측)

| 모델 | 왼쪽 끝에서 | 밑에서 |
|---|---|---|
| BLD1R · BLD1UA **4591** (목대 405) | 74.5 · 202.5 · 330.5 | **30** (가운데) |
| BLD1R · BLD1UA **6091** (목대 555) | 117.5 · 277.5 · 437.5 | **30** (가운데) |
| BLD2R **6062** (목대 555) | 117.5 · 277.5 · 437.5 | **40 과 71** ← 목대마다 다름 |

- 가로는 도면 표의 **`C · B · B · C` 체인** 그대로입니다. **합이 목대 가로와 맞아야 합니다** —
  `117.5 + 160 + 160 + 117.5 = 555` · `74.5 + 128 + 128 + 74.5 = 405`. 고친 뒤 반드시 다시 더해 보십시오.
- ⚠ **`mokdaeBore.y` 는 목대 개수만큼 있습니다.** BLD2R 은 `[40, 71]` 입니다 —
  **도면에 두 조각을 가르는 이름이 없어** 둘 다 적어 드러냅니다. ([`확인대기.md`](확인대기.md) **B23**)
- ⚠ **`oldStdOf` 의 반환에 `mokdaeBore` 를 넣는 것을 빠뜨리지 마십시오.** 그 함수가 칸을 골라
  돌려주므로, 빠뜨리면 자료는 있는데 **화면이 조용히 빕니다.** 실제로 한 번 겪었습니다.
- **`ø10 × 12.5` 는 목대가 아니라 유리도어(하부마이다) 쪽**입니다 — 좌우 바깥날에서 **28.5** 안쪽,
  BLD1R 은 밑에서 68 · 100 · 164. 12-15 에서 BD2R 의 「PVC 인서트(초록)」라 적은 것과 **같은 구멍**입니다
  (도면 레이어가 `HIDDEN` 이라 초록으로 보입니다). 목대 보링(`ø5`)과 섞지 마십시오.
